# PLAN.md (временный, рабочий чек-лист сессии)

**Дата:** 2026-06-30
**Владелец:** Claude (planning/review). Код пишет DeepSeek по workflow CLAUDE.md.

## Контекст сессии
- Поднят контекст: код пишется через **DeepSeek** (Claude — только план/ревью).
- План **76** — точечная докрутка истории фраз после плана 75 (error handling в
  `PhraseHistoryList.vue` + документирование контракта нормализации в `history.rs`).
- Стейджи **06 / 07 / 08** — три связанных UX-задачи поверх CodeMirror/редактора:
  - **06** — табы для параллельных текстов (in-memory, фронт-only).
  - **07** — меню редактора (dropdown: AI / история / онлайн-орфография).
  - **08** — офлайн-орфография (Hunspell/spellbook на бэкенде + CodeMirror lint).

## Принцип работы этой сессии
- **Крупные задачи — через сабагентов** (ревью, арх-ревью, исследование), чтобы не
  забивать основной контекст. Claude-оркестратор держит только выводы/вердикты.
- Сабагенты — `model: sonnet` (т.е. glm-4 по конфигу проекта), read-only где можно.
- Код пишет **DeepSeek** (workflow); Claude — план/ревью.

## Порядок действий

### 1. Обсудить стейджи 06/07/08 (задача #1) — ПЕРЕД планами
Цель: подготовить их «делать» — выявить зависимости/конфликты, зафиксировать порядок и
что каждый stage требует своего plan-файла.

Ключевые выводы для обсуждения (см. ниже «Разбор стейджей»):
- **06 и 07 — два независимых UX, могут сосуществовать** (табы сверху, меню-кнопка в строке).
- **07 зависит от 75** (drafts UI списка фраз) для пункта «История фраз» — но AI-пункты
  можно делать сразу (бэкенд уже есть). Орфография-онлайн в 07 = отдельная крупная подзадача.
- **08 — офлайн-орфография, противоположность онлайн-варианту из 07.** Блокирующий **spike**
  (`spellbook` на русском `suggest`) ДО плана. Словари добавят вес инсталлятору.
- **Общая интегр-точка для 07 и 08 — CodeMirror linter** (`@codemirror/lint`), но 07=онлайн,
  08=офлайн; меню (07) лишь **вызывает** проверку, не содержит логику. Не дублировать.
- Все три — `#[serde(default)]` если трогают `settings.json` (урок `playback_pause`).

### 2. Записать PLAN.md (задача #2) — этот файл. ✅

### 3. Ревью review-prompt + отчёт + правки (задача #3)
- Прогнать `docs/reviews/review-prompt.md` по **текущему git diff + коду плана 75/76**
  (в первую очередь — `PhraseHistoryList.vue`, `usePhraseHistory.ts`, `history.rs`).
- Отчёт → `docs/reviews/review-NNN-2026-06-30.md` (формат из review-prompt.md).
- Поправить **критичные и средние** замечания. MINOR/OPTIMIZE — отметить, правлю по
  возможности. Помнить про rule: код правлю напрямую только если тривиально (план 76
  явно допускает прямую правку); нетривиальное → plan-файл для DeepSeek.

### 4. Арх-ревью через сабагента (задача #4)
- Сабагент (Explore/general-purpose, read-only) проверит:
  - дублирование кода между стейджами 06/07/08 и существующим кодом;
  - расхождение доработок с архитектурой приложения (composables pattern, CSS-vars тема,
    Tauri state managers, `#[serde(default)]` конвенция).
- Вывод: вердикт + список рисков/рекомендаций.

## Разбор стейджей (для обсуждения с пользователем)

| Stage | Суть | Бэкенд? | Объём | Зависимости / риски |
|---|---|---|---|---|
| 06 | Табы (несколько текстов, in-memory) | НЕТ | малый | Отдельная сущность от drafts (75); minimal-mode layout |
| 07 | Меню- dropdown (AI/история/орфография-онлайн) | частично (AI есть) | малый→крупный | 75 для «Истории»; орфография-онлайн = новая крупная |
| 08 | Офлайн-орфография (spellbook+Hunspell) | ДА (новый) | средний | **spike spellbook** (блокер); вес словарей |

Конфликты/пересечения:
- 07 ↔ 08: одинаковый CodeMirror-lint UI, разный источник (сеть vs локаль). Логику не
  дублировать — выделить общий linter-слой, источник данных переключаемый.
- 06 ↔ 75: табы (in-memory рабочие) vs drafts (persistent журнал). Не merge.
- 07 ↔ 73 (autocomplete): AI-коррекция/дописать уже в попапе — меню = явный вызов тех же
  команд, не новая логика.

## Решения пользователя (2026-06-30)
1. **Spell-архитектура:** общий spell-linter во фронте, переключаемый источник
   (офлайн spellbook / онлайн LanguageTool / выкл). Логику не дублировать. ⇒ 07 и 08
   ложатся на **общую** linter-базу; пункт меню (07) лишь вызывает проверку.
2. **Порядок plan-файлов:** **06 → 07(AI-каркас+история) → 08(после spike spellbook)**.
   Онлайн-орфография (07) — последняя/опционально, поверх общего linter-слоя.
3. **Сейчас:** параллельно — (a) code-review плана 75/76 по review-prompt.md через
   сабагента, (b) арх-ревью стейджей 06/07/08 через сабагента.

## Итог (заполняется по мере выполнения)
- [x] Задача #1 — стейджи обсуждены (решения выше)
- [x] Задача #2 — PLAN.md записан
- [x] Задача #3 — ревью (review-018) + арх-ревью (review-019) + правки критичных/средних
  - Сделано: CRITICAL 1,2,4 + MINOR (PhraseHistoryList.vue, usePhraseHistory.ts, history.rs,
    commands/history.rs). vue-tsc 0 ошибок, cargo check 0 ошибок/0 warnings.
  - Осталось DeepSeek (нетривиально): CRITICAL 3 (race spawn_save_phrases) + SECURITY
    (валидация размера фразы/filter). См. docs/plans/76 «Статус выполнения».
- [x] Задача #4 — арх-ревью (review-019)

## Правило итерационного цикла (после реализации планов 06/07/08)
- После **каждого** реализованного плана — снова прогонять code-review (по review-prompt.md)
  **и** арх-ревью через сабагентов (glm-4.7). Это отдельный шаг цикла, не разовый.

## Сформированные планы (по стейджам 06/07/08)

| План | Stage | Суть | Зависимости | Статус |
|---|---|---|---|---|
| **77** | 06 | Табы редактора (in-memory, фронт-only) | нет (план 75 — drafts, отдельная сущность) | round1 CHANGES REQUESTED → round2 в работе |
| **78** | — | Общий spell-linter-слой (база для 07/08) | нет (источник-агностичен) | ✅ round1 APPROVED (0/0 build) |
| **79** | 07 | Меню редактора: AI + история (этап 1) | план 75 (есть), 78 — для орфографии (этап 2) | task готов, ждёт освобождения DeepSeek |
| **80** | 08 | **SPIKE** spellbook на русском (предусловие) | нет, блокирующий | ✅ ПРОЙДЕН (suggest 4/4, 5µs/word, 3.5MB) |
| **81** | 08 | Офлайн-орфография (spellbook + Hunspell) | план 78 (фронт), spike 80 (ПРОЙДЕН) | draft, готов к DeepSeek |

**Прогресс реализации (через DeepSeek):**
- ✅ **78** — spell-linter слой реализован, APPROVED, сборка 0/0.
- 🔄 **77 round2** — правки реактивности (C1-C3 + M1) в работе у DeepSeek.
- ⏳ **79** — task готов, запуск после 77 round2 (оба трогают InputPanel.vue — нельзя параллельно).
- ⏳ **81** — после 79 (бэкенд spellcheck команда + словари в bundle).

## Spike 80 — ПРОЙДЕН (вердикт)
`spellbook v0.4.2`: API `Dictionary::new(aff,dic)` + `check()` + `suggest(word, &mut Vec)`.
Русский suggest: «првиет»→«привет», «тстирование»→«тестирование», «програмирование»→
«программирование» (4/4 точных). 5µs/слово, загрузка 437ms, 3.5MB словарь, без паников.
**Fallback (nuspell-wasm) не нужен.** → план 81 на подходе A (нативный Rust бэкенд).

## Отложенные MINOR-замечания → править после ВСЕХ реализаций (задача #13)
Собрать в один task-файл для DeepSeek после планов 77-81:
- **plan 79 review:** `completeText` использует `isCorrecting` → pulse на correct-button, а не на меню (отдельная индикация для «дописать»).
- **review-018 (план 75/76):**
  - `relativeTime` в `PhraseHistoryList.vue:65-73` → вынести в `utils/time.ts` (дублирование).
  - debounceTimer defensive check (`debounceTimer = null` после clearTimeout).
  - (уже сделано: проверка пустого id в delete_phrase_history, контракт нормализации, PhraseEntry serde-default).
- **review-019 / OPTIMIZE (по возможности):** VecDeque для phrases, lazy loading — отложено (n=200 приемлемо), править только если тривиально.
- **CRITICAL 3 + SECURITY из review-018** (race spawn_save_phrases, валидация размера фразы/filter) — это НЕ MINOR, отдельная нетривиальная задача для DeepSeek (см. ниже).

## Доработки после визуального теста (планы 82-84)
- ✅ **82** — убрать верхнее скругление поля под табами (`borderRadius: '0 0 18px 18px'`).
- ✅ **83** — quick-editor очищает таб-отправитель (захват currentText+senderTabId синхронно).
- ✅ **84** — окно управления: show-функция по образцу soundpanel + хоткей Ctrl+Shift+F7
  + настройка «показывать при запуске». Решает белый квадрат (capture/тема после show).
- Все три реализованы через DeepSeek, ревью APPROVED, сборка 0/0/0. Runtime-проверка в процессе.

## Что осталось сделать DeepSeek (из review-018, нетривиально)
- **CRITICAL 3:** race в `spawn_save_phrases` (+ тот же паттерн для HistoryData/NgramData) —
  debounce/batch ИЛИ `Arc<Mutex<()>>` ИЛИ writer-thread. Отдельный task-файл + round.
- **SECURITY:** валидация размера фразы (`MAX_PHRASE_LENGTH`) + длины `filter`. Объединить.

---

## Сессия 2026-07-08: stages 12-16 → планы 92-95

**Запрос:** 4 направления доработок (persist вкладок; оценка хранилища истории; редизайн
саундпанели; AI в проекте) + переописание проекта. Автономно: stages → план → DeepSeek.

### Решения пользователя (развилки)
1. **Табы persist** → отдельный `tabs.json` (HistoryManager-стиль), НЕ settings.json/localStorage.
2. **История** → **остаться на JSON** (SQLite — оверкилл при ≤200 фраз; roadmap зафиксирован).
3. **SoundPanel** → **гибрид**: наборы-вкладки (Sets) в основном окне + быстрые правки в окне вызова.
4. **AI** → фокус: токен-бенчмарк + карта фичей + AI-орфография (вместо онлайн API). DeepSeek-цена
   делает AI практически бесплатным ($0.004/1000 запросов) — деньги НЕ аргумент, риски = латентность
   + приватность.
5. **Проект** → двойное позиционирование (люди с ограничениями речи + стримеры), толерантный язык,
   история из интервью со стримером aleksa.

### Stage-файлы (research, готовы)
| Stage | Файл | Суть | Решение |
|---|---|---|---|
| 12 | `12-editor-tabs-persistence.md` | persist вкладок | `tabs.json` + новый `TabManager` |
| 13 | `13-history-storage-json-vs-sqlite.md` | JSON vs SQLite | **остаться на JSON** |
| 14 | `14-soundpanel-sets-and-inline-editing.md` | саундпанель | гибрид (Sets + inline-редактирование) |
| 15 | `15-ai-features-map-and-token-benchmark.md` | AI карта + бенчмарк | DeepSeek-провайдер + AI-грамматика (гибрид с hunspell) |
| 16 | `16-project-repositioning.md` | README/позиционирование | двойное позиционирование, aleksa |

### Планы (РЕАЛИЗОВАНЫ + верифицированы, 2026-07-08)
| План | Stage | Суть | Статус | Верификация |
|---|---|---|---|---|
| **92** | 12 | Persist вкладок (`tabs.json` + TabManager) | ✅ APPROVED | cargo 0/0, tabs 5/5 |
| **93** | 15 | DeepSeek-провайдер + AI-грамматика | ✅ APPROVED | cargo 0/0, settings 3/3 |
| **94** | 14-A | Саундпанель — наборы (Sets) | ✅ APPROVED | cargo 0/0, soundpanel 6/6 |
| **95** | 16 | Переописание README/PROBLEMS | ✅ done (прямые правки) | docs, Cargo.toml |

**Итог сборки:** `cargo check` 0 ошибок/0 warnings; `cargo test --lib` **72/72 passed**;
`vue-tsc --noEmit` 0 ошибок; `cargo clippy` чист.

### Цикл DeepSeek — что поймано ревью/тестами (не чекбоксами DeepSeek)
Правило WORKFLOW.md подтвердилось: DeepSeek `[x]` поверхностно верны, но ревью+тесты находили
реальные баги каждый план:
- **92 (CRITICAL):** close-handler DeepSeek звал `destroy()` → ломал tray-поведение (бэкенд делает
  `prevent_close + hide`). Фикс Claude: handler только flush, без destroy.
- **92 (test-race):** тесты делили temp-path (per-PID) → гонка. Фикс: AtomicU64 path.
- **93 (CRITICAL):** `AiDeepSeekSettings::default()` давал `model=""` (derive(Default) + serde-fn) →
  DeepSeek-клиент упал бы у новых/старых конфигов. Поймано тестом миграции. Фикс: явный impl Default.
- **94:** чисто от DeepSeek (6 тестов миграции он добавил сам, все зелёные).
- **pre-existing:** `test_openai_client_has_model_field` не учитывал поле `timeout` — хрупкий
  структурный тест, падал независимо от планов. Фикс: `+ sizeof::<u64>()`.

Отчёты ревью: `docs/deepseek/reviews/9{2,3,4}-round1-claude-review.md`.

### Осталось (runtime, требует ручного/GUI теста — не автоматизируется)
- **92:** создать 2 вкладки + текст → закрыть (в трей) → реальный выход → рестарт → восстановление.
- **93:** реальный вызов DeepSeek API (тестовый ключ локально) — `correct_text`/`get_ai_completion`/
  `ai_check_grammar`. AI-грамматика сейчас по всему тексту (selection — будущий план).
- **94:** создать 2 набора → переключить в основном окне → selector в окне вызова циклит → A играет
  звук активного набора → синхронизация через события. Миграция старого `soundpanel_bindings.json`.

### Порядок запуска (DeepSeek) — выполнен
- ~~92 и 93 — независимо~~ (сделаны последовательно: 92 → 93 → 94 → 95).
- ~~94 — после 92/93~~ ✅. ~~95 — последним~~ ✅.

### Будущие планы (не сформированы — отметить)
- **14-B / ~96:** SoundPanel inline-редактирование (drag-drop + режим редактирования в окне вызова).
  Spike drag-drop подтверждён (`onDragDropEvent.payload.paths`, `dragDropEnabled:true`). После 94.
  ⚠️ **Номер 96 занят** тестовым багом (ниже) — этот план получит **105+**.
- **~97:** AI перефразирование (prompt-вариант `correct`, малый). После 93. ⚠️ Номер 97 занят.
- **SQLite roadmap:** если история > 2000 фраз / FTS / теги — отдельный stage (stage 13 зафиксировал
  триггеры).
- **Авто-озвучка чата (Twitch AI-сводка):** отдельное направление (не сейчас).

---

## Сессия 2026-07-08 (продолж.): планы 96-104 — баги с тестирования

**Запрос:** пользователь тестирует сборку и накидывает баги. Задача: формировать каждый баг в
отдельный план → DeepSeek → **коммитить каждое исправление сразу**. Код пишет DeepSeek, не Claude.

### Сформированные планы (РЕАЛИЗОВАНЫ через DeepSeek, 2026-07-08)
| План | Тип | Суть | Статус | Коммит |
|---|---|---|---|---|
| **96** | bug | Саундпанель пуста при старте (`find_active_index` баг + reload при show) | ✅ | `fbf714e` |
| **97** | bug | A-Z layout-независимо (`e.key` → `e.code`) | ✅ | `3a01908` |
| **98** | bug/UX | Спелл: default ON + тумблер в «Редактор» | ✅ | `aac1261` |
| **99** | feature | Превью `\ключ` через autocomplete-popup | ✅ | `9629312` |
| **100** | bug | Диалог набора → свой модал (без «tauri.localhost») | ✅ | `60d9f4f` |
| **101** | feature | Саундпанель «stay visible» + оставить click-through | ✅ | `11d1fd3` |
| **102** | regression | Окно управления: диагностические логи (нужен runtime-тест) | ⚠️ логи добавлены, фикс ждёт логов | `6dbd054` |
| **103** | bug | Клик в центр редактора снимает фокус (cm-content min-height + focus) | ✅ | `4e8330e` |
| **104** | bug+UX | История: replace/append кнопки + Ctrl+Z (annotation фикс) | ✅ | `ba19c2a`+`62d89df` |

**Итог сборки:** `cargo check` 0/0; `cargo test --lib` **75/75 passed** (72 + 3 новых
find_active_index); `vue-tsc --noEmit` 0 ошибок. Все коммиты тематические, по одному на план.

**Решения пользователя (применены):**
- 98: спелл — default ON, тумблер в разделе «Редактор».
- 99: autocomplete-popup (первый вариант).
- 101: саундпанель «постоянно висящая» → click-through оставляем + опция stay_visible.
- 104: две кнопки (replace/append), дефолт-клик = append.

**102 — требует runtime-теста:** логи в `%APPDATA%\ttsbard\logs` покажут, где обрывается
цепочка (скорее всего аудиоустройство: `PlaybackStarted` эмитится только если sink открылся).
Пользователь воспроизводит → присылает лог → точечный фикс.

### Не закоммичено намеренно
- `.claude/settings.local.json` (local).

### Порядок (предложение)
1. Сначала **чистые баги без решений** → DeepSeek → коммит: **97** (layout), **100** (dialog title),
   **103** (focus), **104-часть2** (Ctrl+Z bug — без UX-решения можно починить только undo).
2. Затем баги с runtime-диагностикой: **96** (soundpanel empty), **102** (playback regression).
3. Затем — требующие решений: **98** (spell default), **99** (preview variant), **101** (click-through),
   **104-часть1** (history append/replace UX).

### Решения пользователя (нужно до запуска соответствующих планов)
- **98:** default spellcheck ON? место тумблера (EditorMenu / своя секция)?
- **99:** popup-autocomplete (рекомендация) vs ghost-text vs hover-tooltip?
- **101:** убрать click-through (A) / перевести A-Z на hook (B) / hint (C) / убрать из основного окна (D)?
  ⚠️ требует факт-чека (A-Z в click-through работают?).
- **104:** дефолт-клик по истории = append (рекомендация) или replace?

### Тестовый ключ DeepSeek
`sk-41451e4ea05a4e75b616ade7df5e36f0` — **только локальный тест**, НЕ коммитить. Для плана 93.

### EditorMenu — ✅ существует
`src/components/editor/EditorMenu.vue` реализован (эмитает `correct`/`complete`/`toggle-history`).
⇒ План 93 (AI-грамматика) интегрируется пунктом в EditorMenu — зависимости удовлетворены.
