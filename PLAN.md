# PLAN.md (временный, рабочий чек-лист сессии)

**Дата:** 2026-06-30
**Владелец:** Claude (planning/review). Код пишет DeepSeek по workflow CLAUDE.md.

## Контекст сессии
- Поднят контекст: код пишется через **DeepSeek** (Claude — только план/ревью).
- План **76** — точечная докрутка истории фраз после плана 75 (error handling в
  `PhraseHistoryList.vue` + документирование контракта нормализации в `history.rs`).
- Стейджи **06 / 07 / 08** — три связанных UX-задачи поверх CodeMirror/редактора:
  - **06** — табы для параллельных текстов (in-memory, фронт-only).
  - **07** — меню редактора (dropdown: AI / история / онлайн-орфография).
  - **08** — офлайн-орфография (Hunspell/spellbook на бэкенде + CodeMirror lint).

## Принцип работы этой сессии
- **Крупные задачи — через сабагентов** (ревью, арх-ревью, исследование), чтобы не
  забивать основной контекст. Claude-оркестратор держит только выводы/вердикты.
- Сабагенты — `model: sonnet` (т.е. glm-4 по конфигу проекта), read-only где можно.
- Код пишет **DeepSeek** (workflow); Claude — план/ревью.

## Порядок действий

### 1. Обсудить стейджи 06/07/08 (задача #1) — ПЕРЕД планами
Цель: подготовить их «делать» — выявить зависимости/конфликты, зафиксировать порядок и
что каждый stage требует своего plan-файла.

Ключевые выводы для обсуждения (см. ниже «Разбор стейджей»):
- **06 и 07 — два независимых UX, могут сосуществовать** (табы сверху, меню-кнопка в строке).
- **07 зависит от 75** (drafts UI списка фраз) для пункта «История фраз» — но AI-пункты
  можно делать сразу (бэкенд уже есть). Орфография-онлайн в 07 = отдельная крупная подзадача.
- **08 — офлайн-орфография, противоположность онлайн-варианту из 07.** Блокирующий **spike**
  (`spellbook` на русском `suggest`) ДО плана. Словари добавят вес инсталлятору.
- **Общая интегр-точка для 07 и 08 — CodeMirror linter** (`@codemirror/lint`), но 07=онлайн,
  08=офлайн; меню (07) лишь **вызывает** проверку, не содержит логику. Не дублировать.
- Все три — `#[serde(default)]` если трогают `settings.json` (урок `playback_pause`).

### 2. Записать PLAN.md (задача #2) — этот файл. ✅

### 3. Ревью review-prompt + отчёт + правки (задача #3)
- Прогнать `docs/reviews/review-prompt.md` по **текущему git diff + коду плана 75/76**
  (в первую очередь — `PhraseHistoryList.vue`, `usePhraseHistory.ts`, `history.rs`).
- Отчёт → `docs/reviews/review-NNN-2026-06-30.md` (формат из review-prompt.md).
- Поправить **критичные и средние** замечания. MINOR/OPTIMIZE — отметить, правлю по
  возможности. Помнить про rule: код правлю напрямую только если тривиально (план 76
  явно допускает прямую правку); нетривиальное → plan-файл для DeepSeek.

### 4. Арх-ревью через сабагента (задача #4)
- Сабагент (Explore/general-purpose, read-only) проверит:
  - дублирование кода между стейджами 06/07/08 и существующим кодом;
  - расхождение доработок с архитектурой приложения (composables pattern, CSS-vars тема,
    Tauri state managers, `#[serde(default)]` конвенция).
- Вывод: вердикт + список рисков/рекомендаций.

## Разбор стейджей (для обсуждения с пользователем)

| Stage | Суть | Бэкенд? | Объём | Зависимости / риски |
|---|---|---|---|---|
| 06 | Табы (несколько текстов, in-memory) | НЕТ | малый | Отдельная сущность от drafts (75); minimal-mode layout |
| 07 | Меню- dropdown (AI/история/орфография-онлайн) | частично (AI есть) | малый→крупный | 75 для «Истории»; орфография-онлайн = новая крупная |
| 08 | Офлайн-орфография (spellbook+Hunspell) | ДА (новый) | средний | **spike spellbook** (блокер); вес словарей |

Конфликты/пересечения:
- 07 ↔ 08: одинаковый CodeMirror-lint UI, разный источник (сеть vs локаль). Логику не
  дублировать — выделить общий linter-слой, источник данных переключаемый.
- 06 ↔ 75: табы (in-memory рабочие) vs drafts (persistent журнал). Не merge.
- 07 ↔ 73 (autocomplete): AI-коррекция/дописать уже в попапе — меню = явный вызов тех же
  команд, не новая логика.

## Решения пользователя (2026-06-30)
1. **Spell-архитектура:** общий spell-linter во фронте, переключаемый источник
   (офлайн spellbook / онлайн LanguageTool / выкл). Логику не дублировать. ⇒ 07 и 08
   ложатся на **общую** linter-базу; пункт меню (07) лишь вызывает проверку.
2. **Порядок plan-файлов:** **06 → 07(AI-каркас+история) → 08(после spike spellbook)**.
   Онлайн-орфография (07) — последняя/опционально, поверх общего linter-слоя.
3. **Сейчас:** параллельно — (a) code-review плана 75/76 по review-prompt.md через
   сабагента, (b) арх-ревью стейджей 06/07/08 через сабагента.

## Итог (заполняется по мере выполнения)
- [x] Задача #1 — стейджи обсуждены (решения выше)
- [x] Задача #2 — PLAN.md записан
- [ ] Задача #3 — ревью + отчёт + правки (сабагент)
- [ ] Задача #4 — арх-ревью (сабагент)
