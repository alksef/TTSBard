# Task 101-round1-01 — Persist Silero voice selected by speaker command

Read `docs/stage/34-silero-voice-and-playback-recent-dedup.md` and `docs/deepseek/plan/101-silero-voice-playback-recent.md`.

## Scope

- `src-tauri/src/commands/telegram.rs`
- focused backend tests only if a suitable test seam exists

## Requirements

1. In `telegram_set_speaker`, after the Telegram `/speaker` operation succeeds, persist the trimmed voice code to `settings.tts.telegram.current_voice_id` using the existing `SettingsManager` persistence path and emit settings-changed using the existing convention.
2. Preserve the current command signature and return behavior.
3. Do not persist the voice when the remote operation fails.
4. Do not change TTS audio, temp files, persistent audio cache, or UI.
5. Preserve the existing `telegram_select_voice` behavior without duplicating unsafe persistence logic unnecessarily.

Run `cargo check --manifest-path src-tauri/Cargo.toml`.
