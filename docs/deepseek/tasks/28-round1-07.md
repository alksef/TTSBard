# Task: restrict TTS response-header logging

In `src-tauri/src/tts/openai.rs`, stop logging arbitrary response-header values. Log only safe allowlisted metadata such as `content-type`, `content-length`, and `server` if needed; for all other headers log the header name without its value, or omit them. Never log `authorization`, `cookie`, `set-cookie`, proxy/auth headers, or arbitrary response-header values. Preserve request behavior. Run `cargo check` from `src-tauri` and do not run workspace-wide formatting.
