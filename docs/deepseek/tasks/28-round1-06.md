# Task: sanitize TTS frontend debug payload

In `src/components/TtsPanel.vue`, replace the existing debug log that passes the full `newSettings` object with safe metadata only (provider and boolean presence flags). Do not log API keys, proxy URLs, access tokens, or the settings object. Preserve behavior. Run `npx vue-tsc --noEmit`; do not run formatters.
