# Task 126-round1-06: Quick editor wording

В `src/components/settings/SettingsEditor.vue` обнови только тексты quick editor:

- описание под заголовком заменить на `Реакция на Enter, Esc`;
- radio label `Возвращать фокус` заменить на `Возвращать фокус предыдущему окну`;
- рядом/под этим вариантом добавить мелкую подпись:
  `Работает только если окно было вызвано по горячей клавише`.

Сохрани существующий radio value `return_focus`, layout и поведение. Не менять backend.
Проверь `npx vue-tsc --noEmit` и `git diff --check`.
