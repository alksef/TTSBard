# Task 110 round 1: restore the shared settings scroll wrapper

The Effects & DSP template currently has incorrect closing-tag placement in
`src/components/audio/AudioEffectsTab.vue`.

Required final structure:

```text
.unified-tab
  .preview-panel-fixed
    .setting-section
  .effects-scroll
    all effects sections
    .dsp-settings-wrapper
    shared .save-section
```

The `.effects-scroll` element must be a sibling of `.preview-panel-fixed`, not
inside it. It must retain `flex: 1 1 auto`, `min-height: 0`, and
`overflow-y: auto` so the settings panels scroll below the fixed preview panel.

Move/fix only the misplaced closing tags and any directly necessary indentation;
do not change save behavior, component APIs, or unrelated styles. Verify with
`npx vue-tsc --noEmit`, `npm run build`, and `git diff --check`.
