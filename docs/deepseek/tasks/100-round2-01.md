# Round 2: ревью Telegram 2FA

Прочитай `docs/deepseek/plan/100-2026-07-17-telegram-2fa-login.md` и проверь текущий diff реализации Telegram 2FA.

Исправь только следующие замечания:

1. В `src/components/TelegramAuthModal.vue` функция `close()` должна очищать локальный ref с 2FA-паролем и выключать его visibility toggle, чтобы пароль не оставался в памяти UI после закрытия модального окна. Сохрани текущее поведение emit.
2. В `src-tauri/src/telegram/client.rs` не логируй полное содержимое общего `SignInError`/ошибки 2FA через `{:?}` или `{}`. Оставь безопасное сообщение/категорию ошибки без token, password и RPC payload. Не меняй полезное поведение обработки ошибок.

После изменений самостоятельно запусти `npx vue-tsc --noEmit` и `cargo check` из `src-tauri`. Не трогай bug report, plan и unrelated files.
