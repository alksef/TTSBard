# Task: frontend debug-only console

Implement the frontend part of `docs/stage/28-secret-safe-logging.md`.

1. Update `src/utils/debug.ts` so every exported debug logging function, including `debugError`, writes to the browser console only when `import.meta.env.DEV` is true. Preserve the existing function API.
2. Replace direct `console.log`, `console.info`, `console.warn`, and `console.error` calls in these files with the corresponding debug helper: `src/components/AudioPanel.vue`, `HotkeysPanel.vue`, `InputPanel.vue`, `MinimalModeButton.vue`, `PreprocessorPanel.vue`, `Sidebar.vue`, `settings/SettingsNetwork.vue`, `tts/FishAudioModelPicker.vue`, `src/composables/useAppSettings.ts`, `useFishImage.ts`, and `useTwitch.ts`. Do not change already-correct debug-helper calls elsewhere.
3. While touching these calls, do not log full settings objects, API keys, access tokens, authorization values, or raw URLs containing credentials. Keep safe status/error category metadata where possible.
4. Do not change backend, DTOs, startup loading, old log files, or add export commands.

Run `npx vue-tsc --noEmit` and inspect the diff. Do not run workspace-wide formatters.
