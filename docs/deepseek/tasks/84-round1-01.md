# Task 84-round1-01: Окно управления воспроизведением — show по хоткею + автозапуск

По плану `docs/plans/84-2026-06-30-playback-window-show.md`. Прочитай план — там полные
требования, точки интеграции, образцы. Это многофайловая задача (Rust + фронт).

## Суть
Окно `playback-control` (очередь воспроизведения) показывается белым квадратом, потому что
нет show-функции (как у soundpanel). Реализовать по образцу soundpanel: show-функция +
хоткей + настройка «показывать при запуске». Окно должно висеть пока не закрыть крестиком.

## Образец — soundpanel (работает корректно)
- `src-tauri/src/soundpanel_window.rs` — `show_soundpanel_window()`: get_webview_window →
  set_position → `window.show()` → (clickthrough) → `set_window_exclude_from_capture` (windows).
- `src-tauri/src/events.rs:33` — `AppEvent::ShowSoundPanelWindow`.
- `src-tauri/src/setup.rs:121-135` — event-loop thread: событие → `show_soundpanel_window()`.
- `src-tauri/src/hotkeys.rs:52-55` — hotkey эмитит `AppEvent::ShowSoundPanelWindow`.

## Что сделать

### 1. НОВЫЙ `src-tauri/src/playback_window.rs` (по образцу soundpanel_window.rs, упрощённо)
```rust
use tauri::{AppHandle, Manager};
use crate::config::WindowsManager;
use tracing::{debug, info};

/// Show playback-control window (like soundpanel show)
pub fn show_playback_window(app_handle: &AppHandle) -> tauri::Result<()> {
    info!(window_type = "playback-control", action = "show", "show_playback_window called");
    if let Some(window) = app_handle.get_webview_window("playback-control") {
        // Применяем сохранённую позицию (если есть — добавить в WindowsManager, см. шаг 6)
        // ... set_position если сохранена
        window.show()?;
        window.set_focus()?;

        // Применяем защиту от захвата ПОСЛЕ показа (как soundpanel)
        #[cfg(windows)]
        {
            use crate::window::set_window_exclude_from_capture;
            if let Some(manager) = app_handle.try_state::<WindowsManager>() {
                let exclude_from_capture = manager.get_global_exclude_from_capture();
                if let Ok(hwnd) = window.hwnd() {
                    debug!(window_type = "playback-control", exclude_from_capture, "Applying exclude from capture");
                    let _ = set_window_exclude_from_capture(hwnd.0 as isize, exclude_from_capture);
                }
            }
        }
        return Ok(());
    }
    Err(tauri::Error::WindowNotFound)
}

/// Hide playback-control window
pub fn hide_playback_window(app_handle: &AppHandle) -> tauri::Result<()> {
    if let Some(window) = app_handle.get_webview_window("playback-control") {
        window.hide()?;
    }
    Ok(())
}
```

### 2. `src-tauri/src/events.rs` — добавить событие
По образцу `ShowSoundPanelWindow` (строка 33):
```rust
/// Показать окно управления воспроизведением
ShowPlaybackControlWindow,
```
И в `as_str` (строки ~110-116): `AppEvent::ShowPlaybackControlWindow => "show-playback-control-window",`

### 3. `src-tauri/src/setup.rs` — обработчик события + автозапуск
- В SoundPanel event-loop thread (или новом) добавить match arm по образцу `setup.rs:133-135`:
```rust
AppEvent::ShowPlaybackControlWindow => {
    info!("[PLAYBACK] Show playback control window");
    let _ = crate::playback_window::show_playback_window(&app_handle_for_soundpanel);
}
```
- В конце `init_app` (после строки ~360, где pb_window set_theme) — автозапуск:
```rust
// Автопоказ окна управления при старте, если включено в настройках
if settings.<show_playback_on_start getter> {
    let _ = crate::playback_window::show_playback_window(app.handle());
}
```
- **Убрать** старый exclude-from-capture блок `setup.rs:566-573` (он теперь в show-функции).
  Оставить только если show-функция не покрывает — но лучше унифицировать в show.

### 4. Хоткей — `src-tauri/src/hotkeys.rs`
Посмотри, как soundpanel hotkey регистрируется (`hotkeys.rs:52-55` эмитит ShowSoundPanelWindow).
Добавить **отдельный глобальный хоткей** для playback-control, по умолчанию напр. `Ctrl+Shift+P`
(или использовать существующую систему регистрации хоткеев — посмотри `register_hotkey` /
`GlobalShortcut` в hotkeys.rs и как назначаются дефолтные хоткеи). Хоткей → emit
`AppEvent::ShowPlaybackControlWindow`.
> Если система хоткеев сложная (per-key bindings через settings), минимально: добавить
> Tauri-команду `toggle_playback_control_window` + показать кнопку в UI (см. шаг 5), а хоткей
> через настройки хоткеев — отдельный этап. Приоритет: show-функция + команда + автозапуск.

### 5. Команда + регистрация
- `src-tauri/src/commands/mod.rs` (или `commands/playback.rs`):
```rust
#[tauri::command]
pub fn toggle_playback_control_window(app_handle: AppHandle) -> Result<(), String> {
    if let Some(window) = app_handle.get_webview_window("playback-control") {
        if window.is_visible().unwrap_or(false) {
            crate::playback_window::hide_playback_window(&app_handle).map_err(|e| e.to_string())
        } else {
            crate::playback_window::show_playback_window(&app_handle).map_err(|e| e.to_string())
        }
    } else {
        Err("playback-control window not found".to_string())
    }
}
```
- Зарегистрировать в `src-tauri/src/lib.rs` invoke_handler.

### 6. Настройка `show_playback_on_start`
- `src-tauri/src/config/settings.rs` — в **ОБЩИХ** настройках (не editor!) добавить поле
  `show_playback_on_start: bool` с `#[serde(default)]` (default false). Найти структуру общих
  настроек (AppSettings или WindowSettings — посмотри где `theme` / оконные настройки).
  Геттер/сеттер по образцу (`set_*` / `get_*` через `update_field` / `cache.read()`).
- `src/types/settings.ts` — добавить поле в соответствующий DTO.
- Фронт UI: найти компонент **общих настроек** (не SettingsAiPanel — там editor; общие — где
  тема/поведение). Добавить чекбокс «Показывать окно управления при запуске».

## Важные ограничения
- Окно `playback-control` в `tauri.conf.json` оставить `visible: false` — НЕ показывается само.
- Show-функция применяет тему/capture ПОСЛЕ show (как soundpanel) — это решает белый квадрат.
- `#[serde(default)]` на новой настройке (урок playback_pause).
- НЕ ломать soundpanel — это образец, копировать паттерн, не менять существующее.

## Критерии готовности
1. `show_playback_window()` / `hide_playback_window()` по образцу soundpanel.
2. Событие `ShowPlaybackControlWindow` + обработчик в setup.
3. Команда `toggle_playback_control_window` (минимум) + хоткей (если система позволяет).
4. Настройка `show_playback_on_start` (общие, `#[serde(default)]`, default false) + UI чекбокс.
5. Окно открывается корректным UI (тема, НЕ белый квадрат) — проверить runtime.
6. `cargo check` + `cargo clippy --lib` + `npx vue-tsc --noEmit` — **0 ошибок, 0 warnings**.
   Запусти все три, приложи результат.

## Не делай
- НЕ менять soundpanel (образец).
- НЕ делать auto-hide по потере фокуса (окно висит пока не закрыть крестиком).
- НЕ переусложнять хоткей-систему — если сложно, команда + кнопка достаточно для первого шага.

После правок — сборка (3 команды), приложи результат. Если warnings — исправь.
