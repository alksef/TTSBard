# Task 128, round 1: resolve `master` into `dev_tts`

The repository is currently in an unresolved merge of `master` into `dev_tts`.
Read `docs/deepseek/plan/128-2026-07-17-integrate-master-into-dev-tts.md`
before editing. Resolve the merge; do not abort it and do not use blanket
`ours`/`theirs` conflict resolution.

## Required outcome

The result must contain both the Piper feature set from the old `dev_tts` tip
and the new master behavior for Telegram, quick editor, and previous-window
focus.

### Preserve from dev_tts

- TTS provider registry and active-provider selection.
- Dynamic Piper model scanning, embedded runtime, provider cards and lazy
  warm-up.
- `ort` / `espeak-rs` dependency support, debug CRT linker handling, and the
  Signalsmith short-PCM fix.
- The `local` to `local_http_server` rename and its compatible behavior.

### Preserve from master

- Telegram 2FA authentication, retry, cancellation/close handling, and error
  state behavior.
- `QuickEditorMode` migration and UI behavior.
- `return_to_previous_window` hotkey, Win32 foreground tracking, command
  registration, config persistence, and HotkeysPanel UI.

### Conflict-resolution guidance

- In shared settings and DTO files, preserve all independently added fields.
  Existing settings JSON must deserialize when fields are absent.
- In `AppState`, retain both `tts_registry` / registry APIs and
  `previous_foreground_hwnd`.
- In command modules and `lib.rs`, register both the Piper selection commands
  and every Telegram/window command from master.
- In UI components, retain both dynamic Piper UI and master’s quick-editor and
  hotkey UI. Keep TypeScript types aligned with Rust DTOs.
- Keep master release history in `CHANGELOG.md`; do not restore the obsolete
  `v1.0.0-alpha*` release entries from dev_tts.
- For add/add task-file conflicts 126 and 127, retain the master task files
  whose filenames carry the `2026-07-17` date and retain the Piper content
  under new non-conflicting task filenames only if it remains useful.

## Stage numbering and CI

- Keep master stages 36 and 37. Rename Piper stage documents to stages 38 and
  39, update headings and all repository references to their new paths.
- Update `.github/workflows/build.yml` so Windows builds locate and export a
  valid `LIBCLANG_PATH` before Tauri runs, and add `Swatinem/rust-cache@v2`
  for `src-tauri`. The workflow must continue to use the configured NSIS
  bundle upload path; remove obsolete MSI release glob if no MSI is configured.

## Checks

Run `git diff --check`, `cargo check` from `src-tauri`, and
`npx vue-tsc --noEmit` from repository root. Report actual command outcomes.
Do not create a commit; leave the merge ready for independent review.
