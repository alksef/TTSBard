# Task 111 round 1: keep the unsaved warning outside the settings scroll

In `src/components/audio/AudioEffectsTab.vue`, move the existing conditional
`.draft-warning` block (`Есть несохранённые изменения.`) out of `.effects-scroll`.

Required order inside `.unified-tab`:

1. `.preview-panel-fixed`
2. conditional `.draft-warning`
3. `.effects-scroll`

The warning must remain conditional on `isDirty || dspDirty`, keep its existing
appearance, and stay visible while the user scrolls the settings panels. Do not
change save/cancel behavior or unrelated layout. Verify with `npx vue-tsc
--noEmit`, `npm run build`, and `git diff --check`.
