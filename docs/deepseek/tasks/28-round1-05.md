# Task: sanitize remaining frontend debug payloads

Remove full settings payloads from existing debug-only logs. Modify only these files:

- `src/App.vue`
- `src/composables/useWebView.ts`
- `src/components/SettingsAiPanel.vue`

Replace logs that pass complete settings objects (`appSettings.settings.value`, `newSettings`, `settings.value`) with safe metadata only: loaded/changed flags, provider, has_token/has_api_key, host or other non-secret status. Do not log API keys, access tokens, MTProxy secrets, proxy URLs with credentials, full settings objects, or raw request/response objects. Preserve behavior and existing debug helper API. Run `npx vue-tsc --noEmit`; do not run formatters.
