# Task 88-round1-01: Настройки окна управления (opacity/цвет/позиция + вкладка sidebar)

По плану `docs/plans/88-2026-06-30-playback-window-settings.md`. Прочитай план + stage
`docs/stage/10-playback-window-settings-analysis.md` (эталон soundpanel с точными file:строка).

## Суть
Перенести на окно `playback-control` настройки как у soundpanel: прозрачность + цвет фона +
сохранение позиции + отдельная вкладка в sidebar (ниже «Звуковой панели»). ЭТАЛОН = soundpanel
(воспроизвести паттерн 1:1, без clickthrough — playback это интерактивное окно).

## Эталон soundpanel (ТОЧНЫЕ ссылки — читай и копируй паттерн)
- **Настройки:** `src-tauri/src/config/windows.rs:19-30` `SoundPanelWindowSettings {x,y,opacity,bg_color,clickthrough}`,
  defaults `windows.rs:49-54`, хранение в `windows.json` через `WindowsManager`.
- **Методы WindowsManager:** `windows.rs:151-164` `get/set_soundpanel_position/opacity/bg_color`.
- **Команды:** `src-tauri/src/soundpanel/bindings.rs:127-188` `sp_get_floating_appearance`,
  `sp_set_floating_opacity`, `sp_set_floating_bg_color`.
- **show с позицией:** `soundpanel_window.rs:14-26`; **hide с сохранением:** `soundpanel_window.rs:85-92`.
- **appearance-update:** `soundpanel_window.rs:56-66` `update_soundpanel_appearance` (window.emit).
- **Фронт overlayStyle:** `src-soundpanel/SoundPanelApp.vue:31-43` `hexToRgba` + computed overlayStyle,
  применён на `.overlay` div; listener `soundpanel-appearance-update`.
- **UI вкладка:** `src/components/SoundPanelTab.vue:319-372` appearance-секция (color picker +
  text #RRGGBB + opacity slider 10-100% + preview-box), функции `saveOpacity/saveBgColor:176-198`,
  загрузка из `appSettings.value.windows.soundpanel.*`.
- **Sidebar:** `src/components/Sidebar.vue:80-114` — группа «Инструменты», пункт soundpanel.
  Зарегистрировать новую вкладку там, где монтируются панели по id (App.vue или эквивалент).

## Что сделать

### 1. Бэкенд `src-tauri/src/config/windows.rs`
- Добавить `PlaybackWindowSettings` (как SoundPanelWindowSettings, БЕЗ clickthrough):
  ```rust
  #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
  pub struct PlaybackWindowSettings {
      #[serde(default)]
      pub x: Option<i32>,
      #[serde(default)]
      pub y: Option<i32>,
      #[serde(default = "default_playback_opacity")]
      pub opacity: u8,
      #[serde(default = "default_playback_bg_color")]
      pub bg_color: String,
  }
  ```
  + `default_playback_opacity() -> u8 { 94 }` + `default_playback_bg_color() -> String { "#10131a".to_string() }`.
- Поле `pub playback: PlaybackWindowSettings` в общей структуре WindowSettings (где `soundpanel`),
  с `#[serde(default)]`.
- WindowsManager методы: `get_playback_position`, `set_playback_position`,
  `get_playback_opacity`, `set_playback_opacity`, `get_playback_bg_color`, `set_playback_bg_color`
  (копия soundpanel-методов, поле `.playback`).

### 2. Команды (новый `src-tauri/src/commands/playback_window.rs` или в commands/mod.rs)
По образцу sp_* (но state можно хранить в WindowsManager напрямую, без отдельного State как
SoundPanelState — НЕ нужно clickthrough):
- `pc_get_appearance(windows_manager) -> (u8, String)` — (opacity, bg_color).
- `pc_set_opacity(value, windows_manager)` → set_playback_opacity + emit `playback-appearance-update`.
- `pc_set_bg_color(color, windows_manager)` → валидация hex (is_valid_hex_color если есть, или
  простая проверка) + set_playback_bg_color + emit.
- `emit` через `app_handle.emit("playback-appearance-update", ())` (глобально — окно в capabilities,
  событие дойдёт) ИЛИ `window.emit` точечно (get_webview_window("playback-control")).
Зарегистрировать команды в `lib.rs invoke_handler`.

### 3. `src-tauri/src/playback_window.rs`
- `show_playback_window`: применять сохранённую позицию ДО show (get_playback_position →
  set_position, как soundpanel_window.rs:14-26).
- `hide_playback_window`: сохранять позицию перед hide (window.outer_position → set_playback_position).
- `update_playback_appearance(app_handle)`: `window.emit("playback-appearance-update", ())`.

### 4. Фронт `src-playback/PlaybackControlApp.vue`
- Refs `opacity` (number), `bgColor` (string). Загрузка в onMounted: `invoke<[number,string]>('pc_get_appearance')`.
- Computed `overlayStyle` (скопировать hexToRgba из SoundPanelApp.vue:36-43) → `backgroundColor: hexToRgba(bgColor, opacity/100)`.
- Применить на корневом div `.playback-window` `:style="{ background: overlayStyle.backgroundColor }"`
  (заменить/дополнить текущий `background: var(--bg)`).
- Listener `playback-appearance-update` → перечитать pc_get_appearance.

### 5. Sidebar + новая вкладка
- `src/components/Sidebar.vue` — в группу «Инструменты» НИЖЕ soundpanel добавить:
  `{ id: 'playback', label: 'Управление воспроизведением', icon: MonitorPlay }` (импортировать MonitorPlay из lucide).
- Новый `src/components/PlaybackTab.vue` — appearance-секция по образцу SoundPanelTab.vue:319-372:
  color picker (input type=color) + text (#RRGGBB) + opacity slider (10-100%) + preview-box.
  Функции saveOpacity/saveBgColor → pc_set_opacity/pc_set_bg_color. Загрузка из
  `appSettings.value.windows.playback.*` (добавить поле в DTO types/settings.ts).
- Зарегистрировать вкладку: найти, где sidebar-ids монтируются в панели (App.vue или роутер),
  добавить `playback` → `<PlaybackTab />`.

### 6. types/settings.ts
- Добавить `PlaybackWindowSettingsDto { x: number|null; y: number|null; opacity: number; bg_color: string }`
  и поле `playback` в WindowSettingsDto (где `soundpanel`).

## Важные ограничения
- `#[serde(default)]` на PlaybackWindowSettings + на поле `playback` (миграция windows.json).
- БЕЗ clickthrough (playback — интерактивное окно).
- НЕ ломать soundpanel (образец, копировать, не менять).
- is_valid_hex_color — посмотри, есть ли в проекте (soundpanel использует в bindings.rs); если
  нет — простая проверка `^#[0-9a-fA-F]{6}$`.

## Критерии готовности
1. Окно playback: opacity + bg_color применяются (overlayStyle), live-обновление.
2. Позиция сохраняется между запусками.
3. В sidebar (ниже «Звуковой панели») — «Управление воспроизведением» → вкладка с color picker +
   opacity slider + preview.
4. windows.json миграция безопасна.
5. `cargo check` + `cargo clippy --lib` + `npx vue-tsc --noEmit` — 0 ошибок, 0 warnings.
6. Runtime: меняешь opacity/цвет → окно обновляется; позиция сохраняется.

После правок — сборка (3 команды), приложи результат.
