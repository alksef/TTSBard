# Stage 27 fixes: reliable compact size persistence

Fix compact window size persistence for `src/components/InputPanel.vue`, `src/components/MinimalModeButton.vue`, and the existing Rust window settings/commands only where needed.

The user can resize the main window while compact mode is active. Its width and height must be clamped to 300..500 and reliably saved to `windows.json`, then used after restart when entering compact mode. Temporary app-driven resizes for opening/closing history must not overwrite the configured dimensions. Exiting compact mode must not save the normal window size as compact dimensions.

Review the current Tauri `onResized` registration and lifecycle. Make saving robust when the user resizes and then immediately toggles mode, and avoid feedback loops. If a backend change is needed, keep it narrowly scoped and preserve existing settings defaults/validation.

Verify with `npx vue-tsc --noEmit`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check`.
