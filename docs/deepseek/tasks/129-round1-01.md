# Task 129 round 1: add Rust cache to CI workflows

Implement `docs/deepseek/plan/129-2026-07-17-github-actions-rust-cache.md`.

Edit only `.github/workflows/ci.yml`. Add `Swatinem/rust-cache@v2` with
`workspaces: src-tauri` to the `check-clippy`, `test-rust`, and `build-check`
jobs. Place each cache step immediately after the Rust toolchain setup. Do not
alter existing commands, triggers, permissions, npm caching, or unrelated
workflow files.

After editing, inspect the diff and verify that all three jobs have the cache
step and that the workflow remains structurally valid. Report the changed file
and verification results.
