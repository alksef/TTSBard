# Task: close remaining backend logging findings

Apply only these two fixes and do not run workspace-wide formatting.

1. In `src-tauri/src/secret_log.rs`, make `safe_url_for_log` stop at the first of `/`, `?`, or `#` after credentials are removed, so a URL such as `https://host?token=secret` cannot retain the query. Add a regression test for query-only URLs.
2. In `src-tauri/src/ai/openai.rs`, `src-tauri/src/ai/deepseek.rs`, and `src-tauri/src/ai/custom.rs`, remove raw `error = %error_msg` from request-failure logs. Keep only safe error category/type and status metadata. Do not log the full async-openai error string, because it may contain endpoint, request, or response details.

Do not change any other behavior or files. Do not run `cargo fmt` from the repository root or crate root because it reformats unrelated existing files. Run the helper tests and `cargo check` from `src-tauri`; report the commands and results.
