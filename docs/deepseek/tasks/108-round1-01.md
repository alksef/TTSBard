# Task: restore DSP UI UTF-8 labels

## Scope

Only the newly extracted DSP components from the current uncommitted refactor:

- `src/components/audio/DspSettings.vue`
- `src/components/audio/EqSettings.vue`
- `src/components/audio/CompressorSettings.vue`
- `src/components/audio/LimiterSettings.vue`

## Problem

The extraction introduced mojibake in Russian UI labels and titles (for example
`Р...`). Restore those strings to the original readable Russian text while
preserving the current component structure, bindings, and behavior.

Use the readable Russian labels already present in the current files (some
strings are correct and some are mojibake) and restore every mojibake string.
Do not modify unrelated files.
Do not write files outside the repository (no `/tmp` or other external paths);
use `git show` output directly or repository-local temporary files only if
needed.

## Verification

- `npx vue-tsc --noEmit`
- `git diff --check`
- inspect the diff to ensure this task changes only text encoding/content in the
  four DSP Vue files and does not revert the split.
