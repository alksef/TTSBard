# Stage 27 review fixes: compact exit ordering

Review fix for `27-round2-03`.

1. In `MinimalModeButton.vue`, flush the pending compact size before incrementing the shared `appDrivenResize` counter, or otherwise allow the intentional flush through. The flush must not be skipped because the mode-toggle guard is already active.
2. When leaving compact mode, remove the native max/min bounds before resizing to the normal 800x630 size. Otherwise the 500 px max can reject the normal resize. Keep the operation guarded and restore a usable state if an invocation fails.
3. Ensure the native 300..500 bounds represent physical window pixels consistently with `resize_main_window` and persisted dimensions. Keep capability/command registration correct.

Run `npx vue-tsc --noEmit`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check`.
