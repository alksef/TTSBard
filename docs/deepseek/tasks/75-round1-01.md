# Task 75 Round 1: Единое хранилище фраз + UI черновиков (Вариант 1)

Реализуй план `docs/deepseek/plan/75-phrase-history-drafts.md` (контекст —
`docs/stage/05-phrase-history.md`). **Пиши код сам** — здесь только точные требования.
Соблюдай существующие паттерны (`parking_lot`, off-thread save, `Result<T,String>`,
**без `.expect()`/паник** в путях журнала). Не доверяй своим чек-боксам — проверяй, что код
компилируется и пути вызовов сходятся.

## Backend — `src-tauri/src/history.rs`
1. Новый тип `PhraseEntry { id: String, text: String, count: u32, last_used: i64 }`
   (`Serialize`/`Deserialize`/`Clone`/`Debug`).
2. `HistoryManager`: добавить `phrase_path: PathBuf` и `phrases: RwLock<Vec<PhraseEntry>>`.
   - `new(path, ngram_path, phrase_path)` — грузит `phrase_history.json` (`unwrap_or_default`).
   - `history_paths()` → возвращает **3** пути: `input_history.json`, `ngrams.json`,
     `phrase_history.json`. Обнови вызов в `src-tauri/src/lib.rs:232`.
   - Off-thread persistence фраз (снимок под локом → drop → write), как `spawn_save`.
3. Методы:
   - `record_phrase(&self, text: &str)`: дедупликация по `text` (case-insensitive `trim`).
     Есть → `count += 1`, `last_used = Utc::now().timestamp()`. Нет → push с uuid v4 id,
     `count = 1`. Лимит N=200 (кольцевой: вытеснять с самым старым `last_used`). Save.
   - `get_phrases(&self, filter: Option<&str>, limit: usize) -> Vec<PhraseEntry>`: фильтр по
     подстроке (case-insensitive в `text`), сортировка `last_used` desc, truncate `limit`.
   - `delete_phrase(&self, id: &str)`, `clear_phrases(&self)` — мутация + save.

## Backend — `src-tauri/src/commands/history.rs` + `lib.rs`
4. Команды (`Result<T, String>`, зарегистрируй в `invoke_handler` после `get_phrase_completion`
   ~lib.rs:277):
   - `get_phrase_history(filter: Option<String>, limit: usize) -> Vec<PhraseEntry>`
   - `delete_phrase_history(id: String)`
   - `clear_phrase_history()`
   `record_phrase` — **не команда**.

## Backend — точка записи (`src-tauri/src/commands/mod.rs::speak_text_internal`, ~81)
5. После успешного `pb.enqueue(...)` (строка ~252) вызови `record_phrase(&text)` ровно **один
   раз** на отправку. Получи `HistoryManager` через `AppState` (рекомендуется прокинуть
   `Arc<HistoryManager>` в `AppState` — `src-tauri/src/state.rs`, по аналогии с
   `playback_manager`). Оставь комментарий, какой способ доступа выбран.

## Backend — рефактор `src-tauri/src/playback.rs` (удалить дубль-журнал)
6. Удали: тип `PhraseEntry`, поле `Shared.phrase_history`, константу `PHRASE_HISTORY_SIZE`,
   метод `add_history`. Убери вызовы `add_history` из `enqueue` (~351) и
   `on_playback_finished` (~431).
7. `PlaybackManager::new` — добавь параметр `Arc<HistoryManager>`, храни в поле. Обнови
   вызов в `src-tauri/src/setup.rs:88` (HistoryManager уже создан раньше в lib.rs — пробрось).
8. `get_state()` (~441): `recent` заполнять из `history.get_phrases(None, 5)`.
   **Приведи `PlaybackStateDto.recent` и фронтенд к единому `PhraseEntry`** (`id, text, count,
   last_used`) — НЕ оставляй старый `{ id, text, timestamp }`.
9. **`replay_from_cache(id)` (~403):** сохрани работоспособность. После удаления in-memory
   журнала текст для replay бери из аудио-кеша + `s.current`/`queue` (`QueuedPhrase.text`), НЕ
   из истории. Если по id текст не найден — без действия (как сейчас).

## Frontend — `src/`
10. Единый TS-тип `PhraseEntry { id: string; text: string; count: number; last_used: number }`
    (в `src/types/` или рядом с composables). Убери старый тип с `timestamp`, если есть.
11. `src/composables/usePhraseHistory.ts` (по образцу `useInputHistory.ts`): `list(filter,
    limit)` с debounce, `remove(id)`, `clear()`.
12. `src/components/PhraseHistoryList.vue`: свёрнут/развёрнут по кнопке; фильтр по подстроке
    (debounce); список — превью текста, `count`, относительное `last_used`; клик → эмит
    `select(text)`; опционально `remove(id)` (×) и `clear()` (с `confirm`). Стили — только
    CSS-переменные `src/styles/variables.css` (dark/light).
13. Подключи `PhraseHistoryList` внутри `InputPanel.vue` рядом с `TtsEditor`. По `select` —
    заменить документ редактора через expose-метод `TtsEditor`; если в редакторе есть
    **отличный** текст — `confirm()` перед затиранием.

## Проверка (сделай сам перед сдачей)
- `touch` изменённых `.rs`, затем `cargo check` — **0 ошибок, 0 warnings**.
- `npx vue-tsc --noEmit` — 0 ошибок.
- Трассировка (не чек-боксы!): (а) `record_phrase` вызывается ровно 1× на отправку и нигде
  больше; (б) `get_state().recent` идёт из `get_phrases(None, 5)`; (в) `replay_from_cache`
  находит текст без in-memory журнала; (г) in-memory `phrase_history` в playback.rs удалён
  полностью (двух журналов нет).

Сдавай: список изменённых файлов + коротко по каждому пункту, что сделано. Не ставь `[x]`
автоматически — только если реально проверил путь кода.
