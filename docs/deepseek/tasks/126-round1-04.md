# Task 126-round1-04: Fix editor settings layout

В `src/components/settings/SettingsEditor.vue` исправь разметку двух строк настроек,
показанных в attached screenshot: label/checkbox и hint должны располагаться вертикально
внутри каждого `.setting-row`, как в `SettingsGeneral.vue` и `SettingsInterface.vue`.

Используй локальный селектор с достаточной специфичностью, например
`.settings-editor .setting-row { display: block; ... }`, чтобы внешние стили `.setting-row`
не превращали строки в горизонтальную grid/flex-разметку. Не меняй template, тексты,
логику и другие настройки.

Проверь `npx vue-tsc --noEmit` и `git diff --check`.
