# Stage 27 fixes: enforce compact bounds and flush final size

Complete the compact window behavior after the previous fixes.

1. Enforce actual native window bounds while compact mode is active: minimum width/height 300 px and maximum width/height 500 px. When leaving compact mode, remove those temporary bounds so the normal 800x630 window can be restored. Use the existing Tauri window API and capability patterns; keep the change narrowly scoped.
2. Before leaving compact mode, flush the current user-resized compact dimensions to `windows.json` if a debounced save is pending. Do not save the normal-mode 800x630 dimensions. Keep app-driven history expansion and mode-toggle resizes excluded from persistence.
3. Preserve the shared compact state and existing restore behavior. Ensure bounds are applied before entering compact and removed after restoring normal mode.

Verify with `npx vue-tsc --noEmit`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `git diff --check`.
