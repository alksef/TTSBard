# Task 123 — Round 1, Iteration 1: Main window transparency, title bar, and Interface tab

Implement the stage 23 plan in the codebase. Execute the changes precisely according to the instructions below.

---

## 1. Tauri Configuration (`src-tauri/tauri.conf.json`)

Change the configuration of the `main` window:
- Set `"decorations": false`
- Add `"transparent": true`

---

## 2. Rust Config Refactoring (`src-tauri/src/config/windows.rs` and `dto.rs`)

1. In `src-tauri/src/config/windows.rs`:
   - Rename `WindowPosition` to `MainWindowSettings` and add:
     - `custom_background: bool` (default: false, using serde default helper `default_main_custom_background`)
     - `opacity: u8` (default: 100, using serde default helper `default_main_opacity`)
     - `bg_color: String` (default: `"#10131a"`, using serde default helper `default_main_bg_color`)
   - Ensure the position `x` and `y` fields are preserved in `MainWindowSettings` with `#[serde(default)]` and `Option<i32>`.
   - In `SoundPanelWindowSettings` and `PlaybackWindowSettings`, add a field:
     - `appearance_source: String` (default: `"own"`, using serde default helper `default_appearance_source`)
   - Add default helpers:
     ```rust
     fn default_main_custom_background() -> bool { false }
     fn default_main_opacity() -> u8 { 100 }
     fn default_main_bg_color() -> String { "#10131a".to_string() }
     fn default_appearance_source() -> String { "own".to_string() }
     ```
   - In `WindowsSettings::validate()`, validate that `self.main.opacity` is within `10..=100`, and other opacities are within `10..=100`. Verify `bg_color` values are valid hex colors using `is_valid_hex_color`.
   - In `WindowsSettings::default()`, set the defaults:
     - `main` default has `custom_background: false, opacity: 100, bg_color: "#10131a"`.
     - `soundpanel` default has `appearance_source: "main".to_string()`.
     - `playback` default has `appearance_source: "main".to_string()`.
     This ensures new installations default to inheriting from the main window.
   - Implement the following setters on `WindowsManager`:
     - `set_main_position(x, y)` (updates MainWindowSettings position)
     - `set_main_custom_background(bool)`
     - `set_main_opacity(u8)`
     - `set_main_bg_color(String)`
     - `set_soundpanel_appearance_source(String)`
     - `set_playback_appearance_source(String)`
     - Keep existing setters for soundpanel and playback position, clickthrough, stay_visible, etc.

2. In `src-tauri/src/config/dto.rs`:
   - Rename/update DTO mappings to match the Rust structs. Update `WindowsSettingsDto` to contain `main: MainWindowSettingsDto`. Update `From` implementations.

---

## 3. Rust Commands (`src-tauri/src/commands/` and `src-tauri/src/soundpanel/`)

1. In `src-tauri/src/commands/window.rs`:
   - Register new Tauri commands:
     - `get_main_appearance(windows_manager: State<'_, WindowsManager>) -> Result<(bool, u8, String), String>`
     - `set_main_custom_background(value: bool, app_handle: AppHandle, windows_manager: State<'_, WindowsManager>) -> Result<(), String>`
     - `set_main_opacity(value: u8, app_handle: AppHandle, windows_manager: State<'_, WindowsManager>) -> Result<(), String>`
     - `set_main_bg_color(color: String, app_handle: AppHandle, windows_manager: State<'_, WindowsManager>) -> Result<(), String>`
     - `get_soundpanel_appearance_source(windows_manager: State<'_, WindowsManager>) -> String`
     - `set_soundpanel_appearance_source(source: String, app_handle: AppHandle, windows_manager: State<'_, WindowsManager>) -> Result<(), String>`
     - `get_playback_appearance_source(windows_manager: State<'_, WindowsManager>) -> String`
     - `set_playback_appearance_source(source: String, app_handle: AppHandle, windows_manager: State<'_, WindowsManager>) -> Result<(), String>`
   - Any time a window setting is updated, emit the `"settings-changed"` event to the app, and emit `"soundpanel-appearance-update"` and `"playback-appearance-update"` so panels update on the fly.
2. In `src-tauri/src/commands/playback_window.rs`:
   - Update `pc_get_appearance`: If `playback.appearance_source == "main"`, resolve background color and opacity using the main window settings.
     - Note: If `main.custom_background` is false, read the active theme from `SettingsManager` (e.g. Light -> `"#fafcff"`, Dark -> `"#090b0f"`).
3. In `src-tauri/src/soundpanel/bindings.rs`:
   - Update `sp_get_floating_appearance` similarly: if `soundpanel.appearance_source == "main"`, resolve color and opacity using main window settings and active theme.
4. In `src-tauri/src/lib.rs`:
   - Add all new Tauri commands to `tauri::generate_handler![...]`.

---

## 4. Frontend: Custom Titlebar & HTML Transparency

1. In `src/styles/base.css`:
   - Make `html` and `body` have `background: transparent !important`.
2. In `src/App.vue`:
   - Add a custom title bar to the top of `.app-container`.
     ```html
     <div class="app-titlebar" data-tauri-drag-region>
       <div class="titlebar-logo" data-tauri-drag-region>
         <span class="titlebar-text" data-tauri-drag-region>TTSBard</span>
       </div>
       <div class="titlebar-controls">
         <button class="titlebar-btn minimize" @click="minimizeWindow" title="Свернуть" aria-label="Свернуть">
           <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
             <line x1="5" y1="12" x2="19" y2="12" />
           </svg>
         </button>
         <button class="titlebar-btn close" @click="closeWindow" title="Закрыть" aria-label="Закрыть">
           <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
             <line x1="18" y1="6" x2="6" y2="18" />
             <line x1="6" y1="6" x2="18" y2="18" />
           </svg>
         </button>
       </div>
     </div>
     ```
   - In `<script setup>`, implement `minimizeWindow()` using `getCurrentWindow().minimize()` and `closeWindow()` using `getCurrentWindow().close()`.
   - Update the dynamic background of `.app-container` via a computed property (e.g. `appStyle`):
     - Take `main.opacity` (scale to 0-1) and `main.bg_color` (or active theme background: Light -> `#fafcff`, Dark -> `#090b0f`).
     - Set `background: var(--app-gradient-line), var(--app-gradient-glow), rgba(r, g, b, opacity)`.
   - Style the `.app-container` to have `flex-direction: column` so titlebar is at the top. Wrap the sidebar and main content in a container `.app-content-wrapper` with `display: flex; flex: 1; overflow: hidden; width: 100%;`.
   - Add clean CSS for `.app-titlebar` (height 36px, vertical centering, padding, flex, color, hover effects on buttons). Make sure it doesn't break sidebar or main layout.

---

## 5. Frontend: Interface Tab (`src/components/settings/SettingsInterface.vue`)

1. Create `src/components/settings/SettingsInterface.vue`:
   - Import `Moon`, `Sun` from `lucide-vue-next` and `invoke` from `@tauri-apps/api/core`.
   - Show theme selection (Moon/Sun radio boxes) which calls `update_theme` (identical to the old one in `SettingsGeneral.vue`).
   - Main Window settings section:
     - Checkbox "Использовать свой цвет" calling `set_main_custom_background`.
     - Color picker calling `set_main_bg_color`.
     - Opacity range slider (10% - 100%) calling `set_main_opacity`.
   - Sound Panel settings section:
     - Select source: "Как у главного окна" / "Собственное" calling `set_soundpanel_appearance_source`.
     - Color picker and Opacity slider (disabled when source is "Как у главного окна").
   - Playback Control settings section:
     - Select source: "Как у главного окна" / "Собственное" calling `set_playback_appearance_source`.
     - Color picker and Opacity slider (disabled when source is "Как у главного окна").
2. Register `SettingsInterface.vue` in `src/components/SettingsPanel.vue`:
   - Add `"interface"` tab type.
   - Add Interface tab button (e.g., using `Sparkles` or `Palette` icon) with label "Интерфейс".
   - Render `<SettingsInterface v-show="activeTab === 'interface'" @show-message="handleMessage" />`.
3. In `src/components/settings/SettingsGeneral.vue`:
   - Remove the Theme selector section and `update_theme` invocation.

---

## 6. Clean Up Floating Windows & Sidebar

1. In `src-soundpanel/SoundPanelApp.vue`:
   - Remove the transparency icon-button in title bar.
   - Remove the transparency control panel (`showTransparencyControl`).
2. In `src/components/PlaybackTab.vue`:
   - Remove all transparency and background color settings.
   - Replace with a clear, localized message informing the user that styling options were consolidated into Settings -> Interface.

---

## 7. Verification

Verify by running:
- `cargo check`
- `npx vue-tsc --noEmit`
Make sure all checks pass before concluding.
