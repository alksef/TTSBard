# Task: fix findings from backend logging review

Fix only the findings from the independent review of `28-round1-01`.

## Required changes

1. Make `safe_url_for_log` return only `scheme://host[:port]` (or `[invalid-url]`), never path, query, fragment, username, or password. Preserve IPv6 host handling. Update its tests to cover a token-like path and credentials in query/fragment.
2. In `src-tauri/src/ai/zai.rs`, log only the safe normalized URL for both the configured base URL and expected endpoint. Never log the original configured URL.
3. Use `mask_secret` in one explicit diagnostic location where limited visibility is useful: the MTProxy secret diagnostic may log the masked value, but must never log the raw secret. Keep first/last four characters and `[MASK]` format.
4. Check all changed backend files for remaining direct logging of API keys, access tokens, proxy URLs, request bodies, authorization headers, or error strings that may include credentials. Replace sensitive error logging with safe status/type metadata where needed.

## Scope and checks

Keep startup DTO, frontend, old log files, and export commands unchanged. Run `cargo fmt` only on changed files, `cargo test secret_log`, and `cargo check`. Review the final diff for unrelated formatting changes; do not modify unrelated files.
