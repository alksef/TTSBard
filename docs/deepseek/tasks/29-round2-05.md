# Task 29 Round 2 Iteration 5 — Transparent audio tab surface

## Goal

Remove the opaque white rectangle behind the Audio tab navigation while preserving the new dedicated preview/settings scroll layout.

## Bounded file

- `src/components/AudioPanel.vue` only

## Required change

- In `.audio-tabs`, replace the solid `background: var(--color-bg-panel)` with a transparent/glass-compatible surface consistent with the app shell. `background: transparent` is preferred; keep `backdrop-filter` only if it does not create a visible white block.
- Do not change `.setting-section` backgrounds, active tab button styling, preview layout, or scroll behavior.
- Preserve dark/light themes and narrow-width behavior.

## Acceptance criteria

- The tab navigation no longer renders as an opaque white strip in light theme.
- Active tab remains visually identifiable.
- `npx vue-tsc --noEmit` passes.
