Реализуй план `docs/deepseek/plan/126-signalsmith-short-pcm.md`.

Работай только с `src-tauri/src/signalsmith/wrapper.rs`, если это возможно.
Для короткого interleaved PCM добавь padding нулевыми кадрами до размера,
который требуется `SignalsmithStretch::exact()` по input/output latency, затем
обрежь результат до длины, соответствующей исходным кадрам. Сохрани текущие
проверки входа и поведение длинных буферов. Добавь regression-тесты для коротких
mono и stereo буферов на 22050 Hz с ненулевыми tempo/pitch параметрами.

Не изменяй UI и не трогай несвязанные файлы. После правки запусти форматирование
и целевые Rust-тесты, сообщи о фактическом результате.
