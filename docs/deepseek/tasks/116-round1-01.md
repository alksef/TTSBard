# Task 116-round1-01: Атомарная и упорядоченная запись истории и вкладок

План: `docs/deepseek/plan/116-2026-07-11-atomic-history-and-tabs-saves.md` (читать обязательно).

## 1. `src-tauri/src/tabs.rs`

### Что изменить
1. Добавь на верхний уровень файла (после импортов) блокировку записи:
   ```rust
   use std::sync::OnceLock;
   use parking_lot::Mutex;
   use std::io::Write;
   use std::path::Path;
   use std::time::{SystemTime, UNIX_EPOCH};

   static TABS_WRITE_LOCK: OnceLock<Mutex<()>> = OnceLock::new();

   fn tabs_write_lock() -> &'static Mutex<()> {
       TABS_WRITE_LOCK.get_or_init(|| Mutex::new(()))
   }
   ```

2. Реализуй функцию атомарной записи (такую же, как в settings.rs):
   ```rust
   fn write_json_atomically(path: &Path, content: &str) -> std::io::Result<()> {
       let parent = path
           .parent()
           .ok_or_else(|| std::io::Error::new(std::io::ErrorKind::NotFound, "No parent dir"))?;

       let stamp = SystemTime::now()
           .duration_since(UNIX_EPOCH)
           .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?
           .as_nanos();
       let tmp_path = parent.join(format!(
           ".{}.{}.tmp",
           path.file_name().and_then(|name| name.to_str()).unwrap_or("tabs.json"),
           stamp
       ));

       {
           let mut file = fs::File::create(&tmp_path)?;
           file.write_all(content.as_bytes())?;
           file.sync_all()?;
       }

       if let Err(rename_error) = fs::rename(&tmp_path, path) {
           let _ = fs::remove_file(path);
           fs::rename(&tmp_path, path).map_err(|e| {
               std::io::Error::new(
                   std::io::ErrorKind::Other,
                   format!("Replace failed: {} (original: {})", e, rename_error),
               )
           })?;
       }

       Ok(())
   }
   ```

3. Удали `spawn_save`.

4. Перепиши метод `save_all` на синхронный вызов `write_json_atomically`:
   ```rust
   pub fn save_all(&self, mut data: TabsData) {
       // ... проверки лимитов и truncate остаются ...
       let mut guard = self.data.write();
       *guard = data.clone();
       drop(guard);

       let _lock = tabs_write_lock().lock();
       if let Ok(content) = serde_json::to_string_pretty(&data) {
           let _ = write_json_atomically(&self.path, &content);
       }
   }
   ```

5. Найди тесты в конце файла `tabs.rs`. Там, где проверялось сохранение, были задержки `std::thread::sleep` (потому что запись была асинхронной). Теперь запись синхронная! Убери `std::thread::sleep(Duration::from_millis(50))` или подобные ожидания из тестов, так как файл записывается сразу же.

---

## 2. `src-tauri/src/history.rs`

### Что изменить
1. Добавь на верхний уровень блокировку и `write_json_atomically` (точно такую же, как для tabs.rs):
   ```rust
   use std::sync::OnceLock;
   use parking_lot::Mutex;
   use std::io::Write;
   use std::path::Path;
   use std::time::{SystemTime, UNIX_EPOCH};

   static HISTORY_WRITE_LOCK: OnceLock<Mutex<()>> = OnceLock::new();

   fn history_write_lock() -> &'static Mutex<()> {
       HISTORY_WRITE_LOCK.get_or_init(|| Mutex::new(()))
   }
   ```

2. Удали функции `spawn_save` и `spawn_save_phrases`.

3. Перепиши все вызовы `spawn_save(...)` и `spawn_save_phrases(...)` на синхронную атомарную запись под блокировкой `history_write_lock().lock()`.

   Для сохранения истории (бывший `spawn_save`):
   ```rust
   fn save_history_sync(path: PathBuf, ngram_path: PathBuf, data: HistoryData, ngrams: NgramData) {
       let _lock = history_write_lock().lock();
       if let Ok(content) = serde_json::to_string_pretty(&data) {
           let _ = write_json_atomically(&path, &content);
       }
       if let Ok(content) = serde_json::to_string_pretty(&ngrams) {
           let _ = write_json_atomically(&ngram_path, &content);
       }
   }
   ```

   Для сохранения фраз (бывший `spawn_save_phrases`):
   ```rust
   fn save_phrases_sync(path: PathBuf, phrases: Vec<PhraseEntry>) {
       let _lock = history_write_lock().lock();
       if let Ok(content) = serde_json::to_string_pretty(&phrases) {
           let _ = write_json_atomically(&path, &content);
       }
   }
   ```

4. Замени вызовы:
   - В `record_text` (строка ~171): `save_history_sync` вместо `spawn_save`.
   - В `clear_history` (строка ~252): `save_history_sync` вместо `spawn_save`.
   - В `record_phrase` (строка ~299): `save_phrases_sync` вместо `spawn_save_phrases`.
   - В `delete_phrase` (строка ~327): `save_phrases_sync` вместо `spawn_save_phrases`.
   - В `clear_phrases` (строка ~337): `save_phrases_sync` вместо `spawn_save_phrases`.

5. Убери все `std::thread::sleep` из тестов в `history.rs` (если они там были для ожидания записи).

---

## Верификация
1. `npx vue-tsc --noEmit` — 0 ошибок.
2. `cargo check` — 0 ошибок (проверь зависимости).
3. В отчёте: покажи изменённый `save_all` в `tabs.rs` и `save_history_sync` в `history.rs`.
