# Task 93-round1-01: DeepSeek-провайдер + AI-грамматика (план 93)

Реализуй план `docs/deepseek/plan/93-2026-07-08-deepseek-provider-and-ai-grammar.md` (прочитай целиком).
Stage-обоснование: `docs/stage/15-ai-features-map-and-token-benchmark.md`.

## Контекст (ключевое открытие для реализации)
**`ZAiClient` (`src-tauri/src/ai/zai.rs`) — это готовый шаблон для DeepSeek.** Он уже реализует
OpenAI-compatible клиент через `async-openai` с **custom base URL** (`.with_api_base(&url)`, строка
zai.rs:90), single-attempt backoff. DeepSeek тоже OpenAI-compatible → `DeepSeekClient` = почти копия
`ZAiClient`, но с **фиксированным** base_url `https://api.deepseek.com/v1` (НЕ из настройки — поле
`url` НЕ нужно, как у OpenAi).

## Часть A — DeepSeek-провайдер (бэкенд)

### A1. `src-tauri/src/config/settings.rs`
1. `AiProviderType` (строки 462-468) — добавить вариант:
   ```rust
   #[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Default)]
   #[serde(rename_all = "lowercase")]
   pub enum AiProviderType {
       #[default]
       OpenAi,
       ZAi,
       DeepSeek,
   }
   ```
   (serde lowercase → `"deepseek"`)
2. Новая структура (по образцу `AiOpenAiSettings`, строки 470-478, БЕЗ поля `url`):
   ```rust
   /// DeepSeek settings (OpenAI-compatible) for AI text correction
   #[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Default)]
   pub struct AiDeepSeekSettings {
       pub api_key: Option<String>,
       #[serde(default)]
       pub use_proxy: bool,
       #[serde(default = "default_deepseek_model")]
       pub model: String,
   }

   fn default_deepseek_model() -> String {
       "deepseek-chat".to_string()
   }
   ```
3. `AiSettings` (строки 497-510) — добавить поле **с `#[serde(default)]`** (бэк-совместимость!):
   ```rust
   #[serde(default)]
   pub deepseek: AiDeepSeekSettings,
   ```
4. `impl Default for AiSettings` (строки 527-537) — добавить `deepseek: AiDeepSeekSettings::default(),`.

### A2. `src-tauri/src/config/dto.rs`
1. `AiProviderTypeDto` (строки 607-612) — добавить `DeepSeek` + в оба `From` impl (614-630):
   `AiProviderType::DeepSeek => AiProviderTypeDto::DeepSeek` и обратно.
2. Новый DTO (по образцу `AiOpenAiSettingsDto`, строки 632-664):
   ```rust
   #[derive(Debug, Clone, Serialize, Deserialize)]
   pub struct AiDeepSeekSettingsDto {
       pub api_key: Option<String>,
       #[serde(default)]
       pub use_proxy: bool,
       #[serde(default = "default_deepseek_model_dto")]
       pub model: String,
   }
   fn default_deepseek_model_dto() -> String { "deepseek-chat".to_string() }
   ```
   + `From<AiDeepSeekSettings> for AiDeepSeekSettingsDto` и обратно (как 646-664).
3. `AiSettingsDto` (строки 700-708) — добавить `pub deepseek: AiDeepSeekSettingsDto,`.
4. Оба `From` impls для `AiSettingsDto` (714-736) — добавить `deepseek: s.deepseek.into(),` и
   `deepseek: dto.deepseek.into(),`.

### A3. Новый файл `src-tauri/src/ai/deepseek.rs`
**Почти копия `src-tauri/src/ai/zai.rs`**, но:
- base_url ФИКСИРОВАННЫЙ (константа `const DEEPSEEK_BASE_URL: &str = "https://api.deepseek.com/v1";`),
  НЕ из настройки.
- Настройки из `settings.deepseek` (api_key, model, use_proxy) — как OpenAi (use_proxy обрабатывается
  как в `openai.rs:46-94` — поддерживает прокси через `network_settings.proxy.proxy_url`).
- Структура `DeepSeekClient { client, model, timeout }`, `impl DeepSeekClient { new(), send_request() }`,
  `impl AiClient for DeepSeekClient { correct() }`.
- Логи/hints переписать под DeepSeek (endpoint `https://api.deepseek.com/v1/chat/completions`).
- Использует те же `ai_common` (DEFAULT_TEMPERATURE, extract_response_content и т.д.).

> ВАЖНО: DeepSeek use_proxy — реализуй ПО ОБРАЗЦУ `openai.rs:46-94` (OpenAi-стиль: строит
> ReqwestClient с прокси), т.к. ZAi-клиент прокси не поддерживает (`_network_settings`).
> Если проще — можно без прокси изначально (как ZAi), но тогда `use_proxy` поле будет
> no-op. Решение: **поддержи прокси** (по openai.rs образцу) — это ценнее. Если сложно —
> оставь use_proxy как no-op с TODO-комментарием, но поле сохрани (для DTO-совместимости).

### A4. `src-tauri/src/ai/mod.rs`
1. `pub mod deepseek;` (рядом с `pub mod zai;`).
2. `AiProvider` enum (строки 94-97) — добавить `DeepSeek(deepseek::DeepSeekClient)`.
3. `impl AiProvider::correct` (строки 101-106) — добавить match arm.
4. `create_ai_client` (строки 114+) — добавить:
   ```rust
   AiProviderType::DeepSeek => {
       let client = deepseek::DeepSeekClient::new(settings, network_settings)?;
       AiProvider::DeepSeek(client)
   }
   ```

### A5. `src-tauri/src/commands/ai.rs`
1. `set_ai_provider` (строки 10-28) — match `"deepseek" => AiProviderType::DeepSeek`.
2. Новые команды (по образцу `set_ai_zai_*`, строки 75-111):
   ```rust
   #[tauri::command]
   pub fn set_ai_deepseek_api_key(settings_manager, state, key: String) -> Result<(), String>
   #[tauri::command]
   pub fn set_ai_deepseek_model(settings_manager, model: String) -> Result<(), String>
   #[tauri::command]
   pub fn set_ai_deepseek_use_proxy(settings_manager, state, enabled: bool) -> Result<(), String>
   #[tauri::command]
   pub fn get_ai_deepseek_model(settings_manager) -> String
   ```
   Каждая: валидация (не пусто для key/model) → `settings_manager.set_*` → `state.invalidate_ai_client()`
   (для key/proxy). Используй `SettingsManager`-сеттеры (см. A6).
3. `get_ai_completion` (строки 217-220, блок `has_key`) — добавить DeepSeek:
   ```rust
   AiProviderType::DeepSeek => settings.ai.deepseek.api_key.is_some(),
   ```

### A6. `src-tauri/src/config/settings.rs` — сеттеры/геттеры
По образцу `set_ai_openai_*` / `set_ai_zai_*` (найди их через grep). Добавь:
- `set_ai_deepseek_api_key(Option<String>)`, `set_ai_deepseek_model(String)`,
  `set_ai_deepseek_use_proxy(bool)`, `get_ai_deepseek_model()`.
- Каждая грузит settings → меняет поле `ai.deepseek.*` → сохраняет (как openai/zai аналоги).

### A7. Регистрация
- `lib.rs` `invoke_handler` — добавь `set_ai_deepseek_api_key`, `set_ai_deepseek_model`,
  `set_ai_deepseek_use_proxy`, `get_ai_deepseek_model`.
- `commands/ai.rs` — если команды экспортируются через `pub use`/`mod` — проверь, что они доступны
  из `lib.rs` (как `set_ai_zai_*`).

## Часть B — AI-грамматика по выделению

### B1. `src-tauri/src/commands/ai.rs` — новая команда
```rust
const MAX_GRAMMAR_TEXT_LEN: usize = 10_000;

const GRAMMAR_PROMPT: &str = "Проверь орфографию и грамматику русского текста. \
    Исправь ошибки, сохранив смысл, стиль и регистр. Верни только исправленный текст \
    без пояснений. Если ошибок нет — верни текст как есть.";

#[tauri::command]
pub async fn ai_check_grammar(
    settings_manager: State<'_, SettingsManager>,
    state: State<'_, AppState>,
    text: String,
) -> Result<String, String> {
    if text.len() > MAX_GRAMMAR_TEXT_LEN {
        return Err(format!("Text too long (max {} chars)", MAX_GRAMMAR_TEXT_LEN));
    }
    let settings = settings_manager.load().map_err(|e| format!("Failed to load settings: {}", e))?;
    let client = state.get_or_create_ai_client(&settings.ai, &settings.tts.network)
        .map_err(|e| format!("AI client error: {}", e))?;
    let result = client.correct(&text, GRAMMAR_PROMPT).await.map_err(|e| e.to_string())?;
    Ok(result)
}
```
- Регистрация в `lib.rs` invoke_handler: `commands::ai::ai_check_grammar`.

## Часть C — Фронт

### C1. `src/types/settings.ts` (AI-настройки)
- `provider` type: добавить `'deepseek'`.
- Новая структура `AiDeepSeekSettings { api_key: string | null; use_proxy: boolean; model: string }`.
- В AI-настройках добавить поле `deepseek: AiDeepSeekSettings` (по образцу openai/zai).
- Найди через grep `zai`/`openai` в этом файле — добавь deepseek симметрично.

### C2. `src/components/SettingsAiPanel.vue`
- В выбор провайдера (radio/select где openai/zai) — добавить вариант DeepSeek.
- Секция настроек DeepSeek (api_key input, model input, use_proxy checkbox) — по образцу OpenAI/Z.ai.
- Сохранение через `invoke('set_ai_deepseek_api_key', {key})`, `set_ai_deepseek_model`, `set_ai_deepseek_use_proxy`.
- Загрузка из `appSettings.ai.deepseek`.

### C3. EditorMenu — пункт «Проверить грамматику (AI)»
`src/components/editor/EditorMenu.vue` существует (эмитает `correct`/`complete`/`toggle-history`).
Добавь новый эмит `grammar` + пункт меню «Проверить грамматику». В `InputPanel.vue` (где обрабатываются
эвенты EditorMenu) — обработчик:
- Берёт **выделение** в CodeMirror (через ref на TtsEditor —暴露 method, или через dispatch напрямую).
  Проще: добавить method `getSelection()` / `applyGrammarResult()` в TtsEditor.vue, ИЛИ передать
  весь текст (если выделения нет) и заменить весь текст.
  **Минимально-жизнеспособный подход**: берёт `text.value` (весь текст, как correct_text), зовёт
  `invoke('ai_check_grammar', { text: text.value })`, заменяет `text.value` результатом. Если есть
  готовый способ получить selection из TtsEditor — используй его; иначе весь текст.
- Loading-индикация (как `isCorrecting`).

> Если интеграция selection сложна — сделай **весь текст** (как existing correct-button), но через
> `ai_check_grammar` (другой prompt — грамматика, не коррекция TTS-чисел). Это уже полезно. Selection
> можно добавить позже. Зафиксируй в комментарии.

## Валидация (ОБЯЗАТЕЛЬНО)
```bash
cd src-tauri && cargo check 2>&1 | tail -20
cd .. && npx vue-tsc --noEmit 2>&1 | tail -20
```
Цель: cargo check **0 ошибок, 0 warnings**; vue-tsc **0 ошибок**. Все match arms покрыты (AiProviderType,
AiProviderTypeDto, AiProvider enum, create_ai_client, From impls) — иначе `non-exhaustive match` errors.

## Чего НЕ делать
- НЕ внедряй LanguageTool/онлайн-API (отказ — stage 15).
- НЕ коммить тестовый ключ `sk-41451e4e...` (только локальный тест).
- НЕ трогай unrelated файлы (вне AI/настроек). Если правишь что-то вне scope — остановись и сообщи.
- НЕ делай непрерывную AI-проверку орфографии (это точечный вызов).

## Самопроверка
1. `AiProviderType::DeepSeek` добавлен во ВСЕ match (settings.rs enum, dto enum+From×2, ai/mod.rs
   AiProvider+correct+create_ai_client, commands set_ai_provider). Иначе не скомпилируется.
2. `#[serde(default)]` на `deepseek` в `AiSettings` — старый settings.json (без deepseek) грузится.
3. DeepSeek base_url = `https://api.deepseek.com/v1` (с `/v1`, без `/chat/completions` — async-openai
   сам добавит).
4. has_key для DeepSeek в get_ai_completion добавлен.
