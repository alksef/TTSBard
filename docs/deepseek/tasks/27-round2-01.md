# Stage 27 fixes: menu placement and compact history behavior

Fix only the frontend behavior in `src/components/editor/EditorMenu.vue`, `src/components/InputPanel.vue`, and related compact-mode wiring.

Requirements:

1. The editor menu dropdown must open to the right of the `⋯` trigger, not to the left under the sidebar. Keep it anchored to the trigger and prevent horizontal overflow where possible.
2. Change the «Озвучить» button tooltip/title to exactly `Enter` (keep an accessible aria-label if useful).
3. If phrase history is expanded when entering compact/minimal mode, close/hide it as part of the transition. Compact mode must show only one history button and no history list until the user opens it again.
4. Compact history expansion must never change the window width. When history opens/closes, preserve the configured compact width and only adjust height within the 300..500 compact-height range. Do not let the temporary expanded size become the persisted compact size.

Preserve existing controlled `v-model:expanded` history behavior. Do not change backend APIs in this task.

Verify with `npx vue-tsc --noEmit` and `git diff --check`.
