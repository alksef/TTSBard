# Task 109 round 1: unify Effects & DSP save controls

Implement the plan in `docs/deepseek/plan/109-unified-audio-save.md`.

Read the current implementations of:

- `src/components/audio/AudioEffectsTab.vue`
- `src/components/audio/DspSettings.vue`
- the settings composables and their save/update APIs in `src/composables/useAppSettings.ts`

Make the smallest coherent refactor:

1. Remove the DSP-local save/cancel toolbar and its toolbar-only props/events from `DspSettings.vue`.
2. Keep DSP editing events and state flow intact.
3. Keep exactly one shared toolbar in `AudioEffectsTab.vue`, rendered after the complete effects and DSP content.
4. Make the shared Save action persist both draft groups and the shared Cancel action restore both groups.
5. Combine saving/disabled/error/success state deliberately; do not create duplicate requests or leave controls enabled during an in-flight save.
6. Preserve the current scroll wrapper, the spacing after the fixed preview panel, and narrow-width behavior.

Do not change unrelated audio behavior or settings persistence formats. Run `npx vue-tsc --noEmit` and `npm run build` after editing, and report the files changed and verification results.
