# Task 105-round1-02: Extract effects tab in small patches

Предыдущая попытка Task 105 не изменила исходники: запись большого файла через shell не сработала. Реализуй тот же extraction, но используй только обычные Edit/apply-patch операции небольшими частями; не создавай временные скрипты и не используй PowerShell/Node для записи больших файлов.

Контекст после Task 104:

- `src/components/AudioPanel.vue` содержит вкладки и effects/DSP-разметку начиная примерно с `<!-- Shared preview panel (fixed above scroll) -->` до конца блока `activeTab === 'effects_dsp'`.
- `src/components/audio/AudioDevicesTab.vue` уже существует и подключён.
- эффекты/DSP script setup находится в `AudioPanel.vue` после device-кода: draftEffects, draftDsp, preview, save/cancel, watchers и связанные функции.

Вынеси effects/DSP вкладку в `src/components/audio/AudioEffectsTab.vue`. Перенеси туда:

1. все effects/DSP refs, computed, watchers и функции;
2. preview выбора/замены/очистки файла и воспроизведения;
3. voice effects, DeepFilterNet и DSP template/style.

После extraction `AudioPanel.vue` должен содержать только оболочку, `activeTab`, tab buttons, `AudioDevicesTab` и `AudioEffectsTab`; device/effects/DSP state не должен дублироваться. Сохрани invoke-команды и поведение. Не выноси ещё EQ/Compressor/Limiter в отдельные файлы — это следующий task.

Проверки: `npx vue-tsc --noEmit`, `git diff --check`. Не форматируй Rust и не изменяй чужие task-файлы.
