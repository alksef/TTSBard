# Task 126-round1-03: Заголовок глобальных горячих клавиш

В `src/components/HotkeysPanel.vue` добавь заголовок `Глобальные` в первый существующий
блок с global hotkeys. Используй тот же визуальный паттерн `section-header`,
`section-title` и иконку `Keyboard`/подходящую уже импортированную иконку, что и для
заголовка `Главное окно`. Не меняй строки, запись, reset, обработчики и backend.

Проверь `npx vue-tsc --noEmit` и `git diff --check`.
