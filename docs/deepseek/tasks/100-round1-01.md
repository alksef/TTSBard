# Task 100-round1-01: Диалог создания набора звуков — свой модал (вместо window.prompt)

План: `docs/deepseek/plan/100-2026-07-08-set-dialog-title.md` (читать).

## Корень
`src/components/SoundPanelTab.vue` `addSet()` (строка ~71-82) использует `window.prompt('Имя
набора:')` — нативный диалог WebView с неконтролируемым заголовком «Сообщение с tauri.localhost».

## Фикс — свой модальный диалог (Вариант A из плана)
В `src/components/SoundPanelTab.vue`:

1. Убрать `window.prompt()` из `addSet()`.
2. Добавить state:
   ```ts
   const showAddSetDialog = ref(false)
   const newSetName = ref('')
   ```
3. `addSet()` — просто открывает модал:
   ```ts
   function addSet() {
     newSetName.value = ''
     showAddSetDialog.value = true
     // фокус на input через nextTick (использовать существующий ref или новый addSetInputRef)
   }
   ```
4. Новый `confirmAddSet()`:
   ```ts
   async function confirmAddSet() {
     const name = newSetName.value.trim()
     if (!name) return
     try {
       const created = await invoke<SoundSet>('sp_add_set', { name })
       await loadSets()
       activeSetId.value = created.id
       bindings.value = []
       showAddSetDialog.value = false
     } catch (e) {
       showError('Ошибка создания набора: ' + (e as Error).message)
     }
   }
   function closeAddSetDialog() {
     showAddSetDialog.value = false
     newSetName.value = ''
   }
   ```
5. Шаблон модала — по образцу существующего диалога добавления звука (`.dialog-overlay` /
   `.dialog`, уже есть в файле около строки 491). Простой модал:
   - заголовок `<h2>Новый набор звуков</h2>`
   - один input (`v-model="newSetName"`, maxlength=50, `@keydown.enter="confirmAddSet"`,
     `@keydown.esc="closeAddSetDialog"`, автофокус)
   - кнопки Отмена (`closeAddSetDialog`) / Создать (`confirmAddSet`, disabled при пустом имени).
   - overlay `@click="closeAddSetDialog"`, inner `@click.stop`.
6. Переиспользовать существующие CSS-классы (`.dialog-overlay`, `.dialog`, `.text-input`,
   `.dialog-actions`, `.cancel-button`, `.save-button`) — НЕ дублировать стили.

## Не трогать
- Backend `sp_add_set`.
- Rename (inline-input в табе — другое).
- Диалог добавления звука (не копировать его логику, только стиль).

## Верификация (DeepSeek прогоняет)
- `npx vue-tsc --noEmit` → 0 ошибок.
- Убедиться, что `invoke`/`SoundSet`/`ref`/`loadSets`/`showError`/`activeSetId`/`bindings` уже
  импортированы/объявлены в файле (они есть) — не дублировать импорты.
