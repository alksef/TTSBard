# Review 29 Round 1 Iteration 1

**Verdict:** CHANGES REQUESTED

## Verified

- `cargo check --manifest-path src-tauri/Cargo.toml` passes.
- `npx vue-tsc --noEmit` passes.
- DSP-focused tests pass: 22/22.
- Settings backward-compatibility test passes.
- Existing audio-effects tests pass: 18/18.
- Full library suite: 172/173; the only failure is the existing `signalsmith::wrapper::tests::test_repeated_calls`, reproduced independently and unrelated to the DSP diff.

## Required follow-up

1. Preview playback currently calls `apply_effects(..., None)` and its `has_effects` condition does not include persisted DSP settings. The UI preview must use the same DSP configuration as the TTS pipeline.
2. Remove the unrelated formatting-only changes in `src-tauri/src/audio/device.rs`.
3. Add the Vue UI as a single DSP parent block containing independent EQ, Compressor, and Limiter subsections, with existing `AudioPanel.vue` styles/patterns used as source of truth.
