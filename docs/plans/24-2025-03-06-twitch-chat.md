# Twitch Chat Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Добавить отправку TTS текста в Twitch чат через IRC подключение, с отдельной панелью настроек аналогично WebView Source.

**Architecture:** Новый модуль `twitch/` с IRC клиентом на tokio-native-tls, интеграция через существующую event систему (AppEvent::TextSentToTts), Vue компонент настроек по образцу WebViewPanel.vue.

**Tech Stack:** Rust (tokio, tokio-native-tls), Tauri 2, Vue 3 + TypeScript, IRC protocol

---

## Task 1: Добавить зависимости для Twitch IRC

**Files:**
- Modify: `src-tauri/Cargo.toml`

**Step 1: Добавить зависимость tokio-native-tls**

Откройте `src-tauri/Cargo.toml` и найдите секцию `[dependencies]`. Добавьте:

```toml
tokio-native-tls = "0.3"
```

Эта библиотека нужна для TLS соединения с Twitch IRC сервером.

**Step 2: Сохранить файл**

**Step 3: Commit**

```bash
git add src-tauri/Cargo.toml
git commit -m "deps: add tokio-native-tls for Twitch IRC TLS connection"
```

---

## Task 2: Создать структуру модуля twitch

**Files:**
- Create: `src-tauri/src/twitch/mod.rs`
- Create: `src-tauri/src/twitch/client.rs`
- Create: `src-tauri/src/twitch/sender.rs`
- Modify: `src-tauri/src/lib.rs`

**Step 1: Создать mod.rs**

```rust
mod client;
pub mod sender;

pub use client::TwitchClient;
pub use sender::send_message;

use serde::{Deserialize, Serialize};

/// Настройки подключения к Twitch IRC
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TwitchSettings {
    pub enabled: bool,
    pub username: String,
    pub token: String,
    pub channel: String,
    pub start_on_boot: bool,
}

impl Default for TwitchSettings {
    fn default() -> Self {
        Self {
            enabled: false,
            username: String::new(),
            token: String::new(),
            channel: String::new(),
            start_on_boot: false,
        }
    }
}

/// Валидация настроек Twitch
impl TwitchSettings {
    pub fn is_valid(&self) -> Result<(), String> {
        if self.username.is_empty() {
            return Err("Username cannot be empty".to_string());
        }
        if self.token.is_empty() {
            return Err("Token cannot be empty".to_string());
        }
        if !self.token.starts_with("oauth:") {
            return Err("Token must start with 'oauth:'".to_string());
        }
        if self.channel.is_empty() {
            return Err("Channel cannot be empty".to_string());
        }
        Ok(())
    }
}
```

**Step 2: Создать client.rs с базовой структурой**

```rust
use super::TwitchSettings;
use std::sync::Arc;
use tokio::sync::{broadcast, RwLock};
use tokio_native_tls::TlsConnector;
use tokio::io::{BufReader, AsyncBufReadExt, AsyncWriteExt};
use tokio::net::TcpStream;
use tracing::{info, error, warn, debug};

/// Статус подключения к Twitch
#[derive(Debug, Clone, PartialEq)]
pub enum TwitchStatus {
    Disconnected,
    Connecting,
    Connected,
    Error(String),
}

/// Twitch IRC клиент
pub struct TwitchClient {
    settings: TwitchSettings,
    status: Arc<RwLock<TwitchStatus>>,
    shutdown_tx: broadcast::Sender<()>,
}

impl TwitchClient {
    /// Создаёт новый клиент Twitch
    pub fn new(settings: TwitchSettings) -> Self {
        let (shutdown_tx, _) = broadcast::channel::<()>(1);

        Self {
            settings,
            status: Arc::new(RwLock::new(TwitchStatus::Disconnected)),
            shutdown_tx,
        }
    }

    /// Запускает IRC подключение
    pub async fn start(&self) -> Result<(), Box<dyn std::error::Error>> {
        *self.status.write().await = TwitchStatus::Connecting;

        info!("[Twitch] Connecting to IRC as {}...", self.settings.username);

        // Подключение к Twitch IRC
        let tcp_stream = TcpStream::connect("irc.chat.twitch.tv:6697").await?;
        let connector = TlsConnector::new();
        let tls_stream = connector.connect("irc.chat.twitch.tv", tcp_stream).await?;

        let (reader, mut writer) = tokio::io::split(tls_stream);
        let mut reader_lines = BufReader::new(reader).lines();
        let mut rx = self.shutdown_tx.subscribe();

        // Авторизация
        let auth_messages = format!(
            "PASS {}\r\nNICK {}\r\nJOIN #{}\r\n",
            self.settings.token, self.settings.username, self.settings.channel
        );
        writer.write_all(auth_messages.as_bytes()).await?;

        info!("[Twitch] Auth sent, waiting for response...");

        // Запуск listener task
        let status_clone = Arc::clone(&self.status);
        let settings_channel = self.settings.channel.clone();

        tokio::spawn(async move {
            let mut reconnect_delay = tokio::time::Duration::from_secs(1);

            loop {
                tokio::select! {
                    // Проверка shutdown сигнала
                    _ = rx.recv() => {
                        info!("[Twitch] Shutdown signal received");
                        *status_clone.write().await = TwitchStatus::Disconnected;
                        break;
                    }
                    // Чтение сообщений от IRC
                    result = reader_lines.next_line() => {
                        match result {
                            Ok(Some(line)) => {
                                debug!("[Twitch] Received: {}", line);

                                // PING/PONG
                                if line.starts_with("PING") {
                                    if let Err(e) = writer.write_all(b"PONG :tmi.twitch.tv\r\n").await {
                                        error!("[Twitch] Failed to send PONG: {}", e);
                                        *status_clone.write().await = TwitchStatus::Error(e.to_string());
                                    }
                                }

                                // Успешный вход (376 или GLHF)
                                if line.contains("376") || line.contains("GLHF") {
                                    info!("[Twitch] Successfully joined #{}", settings_channel);
                                    *status_clone.write().await = TwitchStatus::Connected;
                                    reconnect_delay = tokio::time::Duration::from_secs(1);
                                }

                                // Ошибка авторизации
                                if line.contains("Login authentication failed") {
                                    error!("[Twitch] Authentication failed!");
                                    *status_clone.write().await = TwitchStatus::Error(
                                        "Authentication failed".to_string()
                                    );
                                }
                            }
                            Ok(None) => {
                                warn!("[Twitch] Connection closed by server");
                                *status_clone.write().await = TwitchStatus::Disconnected;
                                break;
                            }
                            Err(e) => {
                                error!("[Twitch] Read error: {}", e);
                                *status_clone.write().await = TwitchStatus::Error(e.to_string());
                                break;
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

    /// Отправляет сообщение в чат Twitch
    pub async fn send_message(&self, text: &str) -> Result<(), Box<dyn std::error::Error>> {
        let status = self.status.read().await;
        if !matches!(*status, TwitchStatus::Connected) {
            return Err("Twitch not connected".into());
        }
        drop(status);

        // TODO: Реализация отправки будет в следующей задаче
        info!("[Twitch] Would send: {}", text);
        Ok(())
    }

    /// Останавливает клиент
    pub async fn stop(self) {
        let _ = self.shutdown_tx.send(());
    }

    /// Возвращает текущий статус
    pub async fn status(&self) -> TwitchStatus {
        self.status.read().await.clone()
    }
}
```

**Step 3: Создать sender.rs (заглушка, будет заполнена позже)**

```rust
use super::client::TwitchClient;

/// Отправляет сообщение через Twitch клиент
pub async fn send_message(client: &TwitchClient, text: &str) -> Result<(), Box<dyn std::error::Error>> {
    client.send_message(text).await
}
```

**Step 4: Зарегистрировать модуль в lib.rs**

Найдите в `src-tauri/src/lib.rs` секцию с модулями и добавьте:

```rust
mod twitch;
```

**Step 5: Commit**

```bash
git add src-tauri/src/twitch/ src-tauri/src/lib.rs
git commit -m "feat: add twitch module structure with IRC client stub"
```

---

## Task 3: Добавить TwitchSettings в AppState

**Files:**
- Modify: `src-tauri/src/state.rs`
- Modify: `src-tauri/src/lib.rs`

**Step 1: Добавить импорт и поле в AppState**

Откройте `src-tauri/src/state.rs` и найдите структуру `AppState`. Добавьте импорт и поле:

```rust
use crate::twitch::TwitchSettings;
```

В структуру `AppState` добавьте поле:

```rust
pub struct AppState {
    // существующие поля...

    /// Настройки Twitch чата
    pub twitch_settings: Arc<RwLock<TwitchSettings>>,
}
```

**Step 2: Обновить создание AppState в lib.rs**

Найдите в `src-tauri/src/lib.rs` функцию `run()` и место где создаётся `AppState`. Добавьте:

```rust
use crate::twitch::TwitchSettings;

// В создании AppState:
let app_state = Arc::new(AppState {
    // существующие инициализации...

    twitch_settings: Arc::new(RwLock::new(TwitchSettings::default())),
});
```

**Step 3: Commit**

```bash
git add src-tauri/src/state.rs src-tauri/src/lib.rs
git commit -m "feat: add twitch_settings to AppState"
```

---

## Task 4: Реализовать отправку сообщений в Twitch IRC

**Files:**
- Modify: `src-tauri/src/twitch/client.rs`

**Step 1: Добавить поле для writer в TwitchClient**

Откройте `src-tauri/src/twitch/client.rs`. Измените структуру:

```rust
use tokio::sync::Mutex;
use tokio::io::WriteHalf;

pub struct TwitchClient {
    settings: TwitchSettings,
    status: Arc<RwLock<TwitchStatus>>,
    shutdown_tx: broadcast::Sender<()>,
    writer: Arc<Mutex<Option<WriteHalf<tokio_native_tls::TlsStream<TcpStream>>>>>,
}
```

**Step 2: Обновить конструктор**

```rust
impl TwitchClient {
    pub fn new(settings: TwitchSettings) -> Self {
        let (shutdown_tx, _) = broadcast::channel::<()>(1);

        Self {
            settings,
            status: Arc::new(RwLock::new(TwitchStatus::Disconnected)),
            shutdown_tx,
            writer: Arc::new(Mutex::new(None)),
        }
    }
```

**Step 3: Обновить метод start для сохранения writer**

```rust
    /// Запускает IRC подключение
    pub async fn start(&self) -> Result<(), Box<dyn std::error::Error>> {
        *self.status.write().await = TwitchStatus::Connecting;

        info!("[Twitch] Connecting to IRC as {}...", self.settings.username);

        let tcp_stream = TcpStream::connect("irc.chat.twitch.tv:6697").await?;
        let connector = TlsConnector::new();
        let tls_stream = connector.connect("irc.chat.twitch.tv", tcp_stream).await?;

        let (reader, writer) = tokio::io::split(tls_stream);
        let mut reader_lines = BufReader::new(reader).lines();
        let mut rx = self.shutdown_tx.subscribe();

        // Сохраняем writer для отправки сообщений
        *self.writer.lock().await = Some(writer);

        // Авторизация через временный writer
        let writer_ref = self.writer.lock().await;
        let writer = writer_ref.as_ref().unwrap();

        let auth_messages = format!(
            "PASS {}\r\nNICK {}\r\nJOIN #{}\r\n",
            self.settings.token, self.settings.username, self.settings.channel
        );
        writer.write_all(auth_messages.as_bytes()).await?;
        drop(writer_ref);

        info!("[Twitch] Auth sent, waiting for response...");

        // Запуск listener task (без writer, он уже сохранён)
        let status_clone = Arc::clone(&self.status);
        let settings_channel = self.settings.channel.clone();

        // Пересоздаём reader так как writer уже сохранён
        let tcp = TcpStream::connect("irc.chat.twitch.tv:6697").await?;
        let tls = connector.connect("irc.chat.twitch.tv", tcp).await?;
        let (r, _) = tokio::io::split(tls);
        let mut reader_lines = BufReader::new(r).lines();

        tokio::spawn(async move {
            // ... остальной код listener без изменений
        });

        Ok(())
    }
```

**Step 4: Реализовать send_message**

```rust
    /// Отправляет сообщение в чат Twitch
    pub async fn send_message(&self, text: &str) -> Result<(), Box<dyn std::error::Error>> {
        let status = self.status.read().await;
        if !matches!(*status, TwitchStatus::Connected) {
            return Err("Twitch not connected".into());
        }
        drop(status);

        // Очистка текста для IRC
        let clean_text = text
            .replace('\n', " ")
            .replace('\r', " ")
            .trim();

        // Ограничение 500 символов
        let clean_text = if clean_text.len() > 500 {
            &clean_text[..500]
        } else {
            clean_text
        };

        let message = format!("PRIVMSG #{} :{}\r\n", self.settings.channel, clean_text);

        let writer_guard = self.writer.lock().await;
        if let Some(writer) = writer_guard.as_ref() {
            writer.write_all(message.as_bytes()).await?;
            info!("[Twitch] Sent: {}", clean_text);
        } else {
            return Err("Writer not available".into());
        }

        Ok(())
    }
```

**Step 5: Commit**

```bash
git add src-tauri/src/twitch/client.rs
git commit -m "feat: implement Twitch IRC message sending"
```

---

## Task 5: Создать Tauri commands для управления Twitch

**Files:**
- Create: `src-tauri/src/commands/twitch.rs`
- Modify: `src-tauri/src/commands/mod.rs`

**Step 1: Создать commands/twitch.rs**

```rust
use crate::settings::AppSettings;
use crate::state::AppState;
use crate::twitch::{TwitchClient, TwitchSettings};
use tauri::{State, Manager};
use std::sync::Arc;

/// Получить текущие настройки Twitch
#[tauri::command]
pub async fn get_twitch_settings(
    state: State<'_, AppState>,
) -> Result<TwitchSettings, String> {
    let settings = state.twitch_settings.read().await;
    Ok(settings.clone())
}

/// Сохранить настройки Twitch и перезапустить клиент если нужно
#[tauri::command]
pub async fn save_twitch_settings(
    settings: TwitchSettings,
    state: State<'_, AppState>,
    app_handle: tauri::AppHandle,
) -> Result<String, String> {
    eprintln!("[TWITCH] Saving settings: enabled={}, start_on_boot={}, channel={}",
        settings.enabled, settings.start_on_boot, settings.channel);

    // Валидация
    if let Err(e) = settings.is_valid() {
        return Err(format!("Validation failed: {}", e));
    }

    // Проверка изменений
    let old_settings = state.twitch_settings.read().await;
    let enabled_changed = old_settings.enabled != settings.enabled;
    let credentials_changed = old_settings.username != settings.username
        || old_settings.token != settings.token
        || old_settings.channel != settings.channel;
    drop(old_settings);

    // Сохранить в AppState
    let mut s = state.twitch_settings.write().await;
    *s = settings.clone();
    drop(s);

    // Сохранить в файлы
    AppSettings::save_twitch_settings(&settings)
        .map_err(|e| format!("Failed to save Twitch settings: {}", e))?;

    // Отправить событие для перезапуска клиента
    if enabled_changed || credentials_changed {
        if let Some(state) = app_handle.try_state::<AppState>() {
            state.send_twitch_event(crate::events::TwitchEvent::Restart);
        }
        Ok("Настройки сохранены. Переподключение...".to_string())
    } else {
        Ok("Настройки сохранены.".to_string())
    }
}

/// Проверить подключение к Twitch
#[tauri::command]
pub async fn test_twitch_connection(
    settings: TwitchSettings,
) -> Result<String, String> {
    // Валидация
    if let Err(e) = settings.is_valid() {
        return Err(format!("Validation failed: {}", e));
    }

    // Тестовое подключение (будет реализовано через отдельную функцию)
    // Для начала просто проверяем валидность
    Ok("Настройки валидны. Попробуйте подключиться.".to_string())
}
```

**Step 2: Зарегистрировать commands в commands/mod.rs**

Добавьте модуль и зарегистрируйте команды:

```rust
pub mod twitch;

// В invoke_handler добавьте:
.invoke_handler(tauri::generate_handler![
    // существующие commands...
    commands::twitch::get_twitch_settings,
    commands::twitch::save_twitch_settings,
    commands::twitch::test_twitch_connection,
])
```

**Step 3: Commit**

```bash
git add src-tauri/src/commands/twitch.rs src-tauri/src/commands/mod.rs
git commit -m "feat: add Tauri commands for Twitch settings management"
```

---

## Task 6: Добавить Twitch события в events.rs

**Files:**
- Modify: `src-tauri/src/events.rs`

**Step 1: Добавить TwitchEvent enum**

Найдите файл `src-tauri/src/events.rs` и добавьте:

```rust
/// События для управления Twitch клиентом
#[derive(Debug, Clone)]
pub enum TwitchEvent {
    /// Перезапустить клиент (изменены настройки)
    Restart,
    /// Остановить клиент
    Stop,
    /// Отправить сообщение
    SendMessage(String),
}
```

**Step 2: Добавить TwitchEvent в AppState**

В том же файле найдите `EventSender` или аналогичный тип и добавьте поддержку:

```rust
pub type TwitchEventSender = broadcast::Sender<TwitchEvent>;
```

**Step 3: Добавить метод для отправки Twitch событий в AppState**

Откройте `src-tauri/src/state.rs` и добавьте в `AppState`:

```rust
use crate::events::{TwitchEvent, TwitchEventSender};

pub struct AppState {
    // существующие поля...

    /// Sender для Twitch событий
    pub twitch_event_tx: TwitchEventSender,
}

// В методах AppState:
impl AppState {
    pub fn send_twitch_event(&self, event: TwitchEvent) {
        let _ = self.twitch_event_tx.send(event);
    }
}
```

**Step 4: Обновить создание AppState в lib.rs**

```rust
let (twitch_event_tx, _) = broadcast::channel::<TwitchEvent>(100);

let app_state = Arc::new(AppState {
    // существующие поля...

    twitch_event_tx,
});
```

**Step 5: Commit**

```bash
git add src-tauri/src/events.rs src-tauri/src/state.rs src-tauri/src/lib.rs
git commit -m "feat: add Twitch event system"
```

---

## Task 7: Добавить сохранение/загрузку настроек Twitch

**Files:**
- Modify: `src-tauri/src/settings.rs`

**Step 1: Добавить пути для Twitch настроек**

В `impl AppSettings` добавьте методы:

```rust
impl AppSettings {
    /// Получить директорию для Twitch настроек
    pub fn twitch_dir(&self) -> PathBuf {
        dirs::config_dir()
            .expect("Failed to get config dir")
            .join("ttsbard")
            .join("twitch")
    }

    /// Получить путь к файлу настроек Twitch JSON
    pub fn twitch_settings_path(&self) -> PathBuf {
        self.twitch_dir().join("settings.json")
    }
```

**Step 2: Добавить методы для загрузки/сохранения**

```rust
    /// Загрузить настройки Twitch из файла
    pub fn load_twitch_settings() -> Result<TwitchSettings, String> {
        use crate::twitch::TwitchSettings;

        let settings_path = Self::twitch_settings_path_static();

        if settings_path.exists() {
            let content = fs::read_to_string(&settings_path)
                .map_err(|e| format!("Failed to read Twitch settings: {}", e))?;

            let settings: TwitchSettings = serde_json::from_str(&content)
                .map_err(|e| format!("Failed to parse Twitch settings: {}", e))?;

            eprintln!("[SETTINGS] Twitch settings loaded: enabled={}", settings.enabled);
            Ok(settings)
        } else {
            eprintln!("[SETTINGS] Twitch settings not found, using defaults");
            Ok(TwitchSettings::default())
        }
    }

    /// Сохранить настройки Twitch в файл
    pub fn save_twitch_settings(settings: &TwitchSettings) -> Result<(), String> {
        let twitch_dir = Self::twitch_settings_path_static()
            .parent()
            .unwrap()

            .to_path_buf();

        fs::create_dir_all(&twitch_dir)
            .map_err(|e| format!("Failed to create Twitch directory: {}", e))?;

        let settings_path = Self::twitch_settings_path_static();
        let json = serde_json::to_string_pretty(settings)
            .map_err(|e| format!("Failed to serialize Twitch settings: {}", e))?;

        fs::write(&settings_path, json)
            .map_err(|e| format!("Failed to write Twitch settings: {}", e))?;

        eprintln!("[SETTINGS] Twitch settings saved");
        Ok(())
    }

    /// Статический метод для получения пути (для использования в статических контекстах)
    fn twitch_settings_path_static() -> PathBuf {
        dirs::config_dir()
            .expect("Failed to get config dir")
            .join("ttsbard")
            .join("twitch")
            .join("settings.json")
    }
```

**Step 3: Добавить импорт**

```rust
use crate::twitch::TwitchSettings;
```

**Step 4: Commit**

```bash
git add src-tauri/src/settings.rs
git commit -m "feat: add Twitch settings persistence"
```

---

## Task 8: Создать Vue компонент TwitchPanel

**Files:**
- Create: `src/components/TwitchPanel.vue`
- Modify: `src/App.vue`

**Step 1: Создать TwitchPanel.vue**

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { invoke } from '@tauri-apps/api/core'

interface TwitchSettings {
  enabled: boolean
  username: string
  token: string
  channel: string
  start_on_boot: boolean
}

const settings = ref<TwitchSettings>({
  enabled: false,
  username: '',
  token: '',
  channel: '',
  start_on_boot: false,
})

const errorMessage = ref<string | null>(null)
let errorTimeout: number | null = null

async function loadSettings() {
  try {
    const loaded = await invoke<TwitchSettings>('get_twitch_settings')
    settings.value = loaded
  } catch (e) {
    showError('Failed to load settings: ' + (e as Error).message)
  }
}

async function save() {
  try {
    console.log('[Twitch] Saving settings:', settings.value)
    const result = await invoke<string>('save_twitch_settings', { settings: settings.value })
    console.log('[Twitch] Save result:', result)
    showError(result)
  } catch (e) {
    console.error('[Twitch] Save failed:', e)
    showError('Failed to save settings: ' + (e as Error).message)
  }
}

async function startTwitch() {
  console.log('[Twitch] Starting...')
  settings.value.enabled = true
  await save()
}

async function stopTwitch() {
  console.log('[Twitch] Stopping...')
  settings.value.enabled = false
  await save()
}

async function testConnection() {
  try {
    const result = await invoke<string>('test_twitch_connection', { settings: settings.value })
    showError(result)
  } catch (e) {
    showError('Test failed: ' + (e as Error).message)
  }
}

function showError(message: string) {
  errorMessage.value = message
  if (errorTimeout !== null) {
    clearTimeout(errorTimeout)
  }
  errorTimeout = window.setTimeout(() => {
    errorMessage.value = null
    errorTimeout = null
  }, 3000)
}

onMounted(async () => {
  await loadSettings()
})

// Cleanup
import { onUnmounted } from 'vue'
onUnmounted(() => {
  if (errorTimeout !== null) {
    clearTimeout(errorTimeout)
  }
})
</script>

<template>
  <div class="twitch-panel">
    <h1>Twitch Chat</h1>

    <!-- Error/Info Message Display -->
    <div v-if="errorMessage" class="message-box" :class="{
      error: errorMessage.includes('Failed') || errorMessage.includes('failed'),
      success: errorMessage.includes('saved') || errorMessage.includes('валид')
    }">
      {{ errorMessage }}
    </div>

    <section class="settings-section">
      <h2>Connection</h2>

      <div class="setting-row server-actions">
        <button
          @click="startTwitch"
          class="start-button"
          :disabled="settings.enabled"
          :class="{ disabled: settings.enabled }"
        >
          ▶ Start
        </button>
        <button
          @click="stopTwitch"
          class="stop-button"
          :disabled="!settings.enabled"
          :class="{ disabled: !settings.enabled }"
        >
          ■ Stop
        </button>
      </div>

      <div class="setting-row">
        <label class="checkbox-label">
          <input type="checkbox" v-model="settings.start_on_boot" />
          <span>Start on boot</span>
        </label>
      </div>

      <div class="setting-row">
        <label>Username:</label>
        <input
          type="text"
          v-model="settings.username"
          class="text-input"
          placeholder="your_bot_username"
        />
      </div>

      <div class="setting-row">
        <label>Token:</label>
        <input
          type="password"
          v-model="settings.token"
          class="text-input"
          placeholder="oauth:xxxxxxxxxxxxxx"
        />
      </div>

      <div class="setting-row">
        <label>Channel:</label>
        <input
          type="text"
          v-model="settings.channel"
          class="text-input"
          placeholder="your_channel"
        />
      </div>

      <div class="setting-row">
        <label>Test:</label>
        <button @click="testConnection" class="test-button">Test Connection</button>
      </div>
    </section>

    <section class="settings-section help-section">
      <h2>Help</h2>
      <p class="help-text">
        Get your OAuth token from:
      </p>
      <a href="https://twitchtokengenerator.com" target="_blank" class="help-link">
        https://twitchtokengenerator.com
      </a>
      <p class="help-text">
        Token format: <code>oauth:xxxxxxxxxxxxxxx</code> (include "oauth:" prefix)
      </p>
    </section>

    <section class="settings-section actions-section">
      <button @click="save" class="save-button">💾 Save Settings</button>
    </section>
  </div>
</template>

<style scoped>
.twitch-panel {
  max-width: 800px;
  margin: 0 auto;
}

h1 {
  margin-bottom: 2rem;
  color: #333;
}

h2 {
  margin-top: 0;
  margin-bottom: 1rem;
  font-size: 1.1rem;
  color: #333;
  font-weight: 600;
}

.message-box {
  padding: 1rem;
  margin-bottom: 1rem;
  border-radius: 8px;
  animation: slideDown 0.3s ease-out;
}

.message-box.success {
  background: #e8f5e9;
  border: 1px solid #c8e6c9;
  color: #2e7d32;
}

.message-box.error {
  background: #fee;
  border: 1px solid #fcc;
  border-left: 4px solid #f44;
  color: #c33;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.settings-section {
  margin-bottom: 1.5rem;
  padding: 1.5rem;
  background: #f5f5f5;
  border-radius: 8px;
}

.setting-row {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 1rem;
  flex-wrap: wrap;
}

.setting-row.server-actions {
  gap: 1rem;
  padding: 1rem;
  background: #f8f9fa;
  border-radius: 8px;
  border: 1px solid #e0e0e0;
}

.setting-row label {
  min-width: 100px;
  font-weight: 500;
  color: #555;
  font-size: 14px;
}

.checkbox-label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
  min-width: auto !important;
}

.checkbox-label input[type="checkbox"] {
  width: 18px;
  height: 18px;
  cursor: pointer;
}

.text-input {
  flex: 1;
  max-width: 400px;
  padding: 0.5rem;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 14px;
  background: #fff;
}

.text-input:focus {
  outline: none;
  border-color: #9146FF;
}

.start-button,
.stop-button,
.test-button,
.save-button {
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 500;
  font-size: 14px;
  transition: all 0.2s;
}

.start-button {
  background: #4CAF50;
  color: white;
}

.start-button:hover:not(.disabled) {
  background: #45a049;
  transform: translateY(-1px);
}

.stop-button {
  background: #2196F3;
  color: white;
}

.stop-button:hover:not(.disabled) {
  background: #1976D2;
  transform: translateY(-1px);
}

.test-button {
  background: #FF9800;
  color: white;
}

.test-button:hover {
  background: #F57C00;
  transform: translateY(-1px);
}

.save-button {
  width: 100%;
  padding: 1rem;
  background: #9146FF;
  color: white;
  font-size: 16px;
}

.save-button:hover {
  background: #772CE8;
  transform: translateY(-1px);
}

.start-button.disabled,
.stop-button.disabled {
  background: #ccc;
  cursor: not-allowed;
  opacity: 0.6;
}

.start-button.disabled:hover,
.stop-button.disabled:hover {
  background: #ccc;
  transform: none;
}

.help-section {
  background: #fff9e6;
  border: 1px solid #ffe082;
}

.help-text {
  margin: 0.5rem 0;
  color: #666;
  font-size: 14px;
}

.help-link {
  color: #9146FF;
  text-decoration: none;
  font-weight: 500;
}

.help-link:hover {
  text-decoration: underline;
}

.help-text code {
  background: #f5f5f5;
  padding: 0.2rem 0.4rem;
  border-radius: 4px;
  font-family: monospace;
}

.actions-section {
  background: transparent;
  padding: 0;
}
</style>
```

**Step 2: Добавить TwitchPanel в App.vue**

Найдите в `src/App.vue` место где определены панели (обычно через компонент Sidebar) и добавьте:

```vue
<script setup>
// Добавьте импорт
import TwitchPanel from './components/TwitchPanel.vue'

// В секции currentPanel добавьте:
const currentPanel = ref<'settings' | 'audio' | 'preprocessor' | 'tts' | 'floating' | 'sound' | 'info' | 'webview' | 'twitch'>('settings')
</script>

<template>
  <!-- В Sidebar добавьте пункт -->
  <SidebarItem label="Twitch Chat" icon="📺" panel="twitch" />

  <!-- В условном рендеринге добавьте -->
  <TwitchPanel v-if="currentPanel === 'twitch'" />
</template>
```

**Step 3: Commit**

```bash
git add src/components/TwitchPanel.vue src/App.vue
git commit -m "feat: add TwitchPanel Vue component with settings UI"
```

---

## Task 9: Интегрировать отправку в event loop

**Files:**
- Modify: `src-tauri/src/lib.rs`

**Step 1: Добавить обработку Twitch событий в event loop**

Найдите в `src-tauri/src/lib.rs` место где обрабатываются события (обычно после создания event_tx). Добавьте:

```rust
use crate::events::TwitchEvent;

// В event loop добавьте обработку Twitch событий:
let mut twitch_rx = app_state.twitch_event_tx.subscribe();

tokio::spawn(async move {
    let mut twitch_client: Option<TwitchClient> = None;

    loop {
        tokio::select! {
            // Обработка Twitch событий
            event = twitch_rx.recv() => {
                match event {
                    TwitchEvent::Restart => {
                        eprintln!("[TWITCH] Restart event received");
                        // Остановить текущий клиент
                        if let Some(client) = twitch_client.take() {
                            client.stop().await;
                        }

                        // Запустить новый если включен
                        let settings = app_state.twitch_settings.read().await;
                        if settings.enabled {
                            if let Ok(()) = settings.is_valid() {
                                let client = TwitchClient::new(settings.clone());
                                let _ = client.start().await;
                                twitch_client = Some(client);
                            }
                        }
                    }
                    TwitchEvent::Stop => {
                        eprintln!("[TWITCH] Stop event received");
                        if let Some(client) = twitch_client.take() {
                            client.stop().await;
                        }
                    }
                    TwitchEvent::SendMessage(text) => {
                        if let Some(client) = &twitch_client {
                            let _ = client.send_message(&text).await;
                        }
                    }
                    _ => {}
                }
            }
            // ... существующие event handlers
        }
    }
});
```

**Step 2: Добавить отправку в Twitch при TextSentToTts событии**

В обработчике `AppEvent::TextSentToTts` добавьте:

```rust
AppEvent::TextSentToTts(text) => {
    eprintln!("[EVENT] Text sent to TTS: {}", text);

    // WebView broadcast (существует)
    if let Some(webview) = &webview_server {
        webview.broadcast_text(text.clone()).await;
    }

    // Twitch send (новое)
    let settings = app_state.twitch_settings.read().await;
    if settings.enabled {
        drop(settings);
        let _ = app_state.send_twitch_event(TwitchEvent::SendMessage(text));
    }
}
```

**Step 3: Commit**

```bash
git add src-tauri/src/lib.rs
git commit -m "feat: integrate Twitch sending into event loop"
```

---

## Task 10: Загрузка настроек при старте приложения

**Files:**
- Modify: `src-tauri/src/lib.rs`

**Step 1: Загрузить Twitch настройки при запуске**

Найдите место где загружаются настройки (обычно `SettingsManager::new().load()`) и добавьте:

```rust
use crate::settings::AppSettings;

// Загрузка Twitch настроек
let twitch_settings = AppSettings::load_twitch_settings()
    .unwrap_or_default();

*app_state.twitch_settings.write().await = twitch_settings.clone();
```

**Step 2: Автозапуск если start_on_boot**

После загрузки настроек добавьте:

```rust
// Автозапуск Twitch если настроен
if twitch_settings.start_on_boot && twitch_settings.enabled {
    if let Ok(()) = twitch_settings.is_valid() {
        eprintln!("[TWITCH] Auto-starting on boot");
        let client = TwitchClient::new(twitch_settings);
        tokio::spawn(async move {
            let _ = client.start().await;
        });
    }
}
```

**Step 3: Commit**

```bash
git add src-tauri/src/lib.rs
git commit -m "feat: load Twitch settings on startup and support start_on_boot"
```

---

## Task 11: Тестирование вручную

**Step 1: Запустить приложение**

```bash
cd src-tauri
cargo run
```

**Step 2: Открыть панель Twitch Chat**

**Step 3: Получить OAuth токен**

1. Перейти на https://twitchtokengenerator.com
2. Выбрать "Chat Bot" scope
3. Скопировать токен (включая "oauth:" префикс)

**Step 4: Заполнить настройки**

- Username: ваш бот логин
- Token: oauth:xxxxxxxxxxxxxxx
- Channel: ваш канал (обычно = username)

**Step 5: Нажать "Test Connection"**

Должно появиться сообщение что настройки валидны.

**Step 6: Нажать "Start"**

Должна измениться кнопка (Stop становится активной).

**Step 7: Проверить логи**

В консоли должно быть:
```
[Twitch] Connecting to IRC as botname...
[Twitch] Auth sent, waiting for response...
[Twitch] Successfully joined #channel
```

**Step 8: Отправить тестовый TTS**

Нажать глобальный хоткей, ввести текст, отправить.

**Step 9: Проверить Twitch чат**

Открыть https://www.twitch.tv/popout/ваш_канал/chat?popout=

Текст должен появиться в чате.

**Step 10: Проверить переподключение**

Остановить интернет, подождать, восстановить - клиент должен переподключиться.

---

## Checklist перед завершением

- [ ] Все тесты проходят
- [ ] IRC подключение устанавливается
- [ ] Сообщения появляются в Twitch чате
- [ ] Start/Stop кнопки работают
- [ ] start_on_boot работает
- [ ] Настройки сохраняются между перезапусками
- [ ] Переподключение при обрыве работает
- [ ] Сообщения об ошибках корректны
- [ ] UI показывает правильный статус

---

## Полезные команды

```bash
# Сборка для релиза
cd src-tauri && cargo build --release

# Запуск с логами
RUST_LOG=debug cargo run

# Проверить порт (если используется)
netstat -an | findstr 6697  # Windows
```

---

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| "Authentication failed" | Проверить токен, убедиться что включает "oauth:" |
| Сообщения не появляются | Проверить что канал совпадает с username |
| Переподключение не работает | Проверить логи, может быть проблема с network |
| UI не обновляется | Проверить что события правильно emit'ятся |
