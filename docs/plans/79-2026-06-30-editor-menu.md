# Plan 79: Меню редактора (AI-каркас + история фраз) — Stage 07, этап 1

**Дата:** 2026-06-30
**Статус:** draft (для реализации через DeepSeek по WORKFLOW)
**Связано:** stage `docs/stage/07-editor-menu-ai-history-spellcheck.md`,
арх-ревью `docs/reviews/review-019-2026-06-30.md`, план 75 (PhraseHistoryList — есть),
план 78 (spellcheck-linter-слой — база), план 73 (autocomplete — не дублировать).

## Контекст
Stage 07 = dropdown-меню из кнопки в InputPanel с пунктами: AI-коррекция, AI-дописать,
история фраз, проверка орфографии. Этот план — **этап 1: каркас меню + AI-пункты + пункт
истории**. Онлайн-орфография (самый дорогой кусок) **исключена** — она отдельным планом
поверх плана 78.

Ключевое: **переиспользуем существующие бэкенд-команды** (`correct_text`, `get_ai_completion`)
и существующий UI истории (`PhraseHistoryList`, план 75). **Не дублируем логику.**

## Цели
1. Dropdown/popover из кнопки в InputPanel.
2. Пункты «AI: корректировать» и «AI: дописать» — обёртки над существующими командами.
3. Пункт «История фраз» — открывает/сворачивает существующий `PhraseHistoryList`
   (переиспользование, не новый UI).
4. Popover: click-outside-to-close, escape, тема CSS-vars, minimal-mode.
5. Каркас масштабируется — добавление пунктов = добавление строк.

## Точные точки интеграции (из research)
- `InputPanel.vue:211-221` — `correct-button` (absolute bottom-right, `--color-accent`).
  Меню-кнопка ставится **рядом** (или заменяет `correct-button`, перенося «AI» внутрь меню).
- `InputPanel.vue:131-142` — `correctText()` → `invoke('correct_text')`. **Переиспользовать.**
- `TtsEditor.vue:176-200` — autocomplete уже вызывает `invoke('get_ai_completion', { context })`.
  Меню = **явный** вызов той же команды. Не дублировать логику.
- `InputPanel.vue:210` — `<PhraseHistoryList @select="selectPhrase" />`. **Переиспользовать**
  для пункта «История».
- **Popover-компонента в проекте НЕТ** (research). Есть паттерн `@click.self` для модалок
  (`TelegramAuthModal.vue:113`, `FishAudioModelPicker.vue:115`). Директивы click-outside НЕТ.
  ⇒ popover делать с нуля (click-outside через слушатель `document`, escape через keydown).
- Образец темизации: везде CSS-vars (`--color-bg-elevated`, `--color-border-strong`,
  `--shadow-soft`).

## Архитектура

### 1. Компонент `src/components/editor/EditorMenu.vue` (НОВЫЙ)
Dropdown-меню. Props: открытые флаги (isAiButtonEnabled, есть ли фразы). Emits: `correct`,
`complete`, `toggle-history`.

**Шаблон (каркас):**
```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'

const emit = defineEmits<{
  correct: []
  complete: []
  'toggle-history': []
}>()

defineProps<{
  isAiEnabled: boolean
  hasText: boolean
}>()

const open = ref(false)

function toggle() { open.value = !open.value }
function close() { open.value = false }

function onDocClick(e: MouseEvent) {
  // click-outside: если клик не внутри [data-editor-menu], закрыть.
  const el = (e.target as HTMLElement).closest('[data-editor-menu]')
  if (!el) close()
}
function onKey(e: KeyboardEvent) { if (e.key === 'Escape') close() }

onMounted(() => {
  document.addEventListener('click', onDocClick)
  document.addEventListener('keydown', onKey)
})
onUnmounted(() => {
  document.removeEventListener('click', onDocClick)
  document.removeEventListener('keydown', onKey)
})

function run(fn: () => void) { close(); fn() }
</script>

<template>
  <div class="editor-menu" data-editor-menu>
    <button class="menu-trigger" @click="toggle" title="Меню редактора">
      <!-- иконка меню, например MoreHorizontal из lucide -->
    </button>
    <div v-if="open" class="menu-dropdown">
      <button class="menu-item" :disabled="!hasText || !isAiEnabled" @click="run(() => emit('correct'))">
        AI: корректировать
      </button>
      <button class="menu-item" :disabled="!hasText || !isAiEnabled" @click="run(() => emit('complete'))">
        AI: дописать
      </button>
      <div class="menu-separator" />
      <button class="menu-item" @click="run(() => emit('toggle-history'))">
        История фраз
      </button>
    </div>
  </div>
</template>
```
**Click-outside:** через `document` listener + `data-editor-menu` маркер (надёжнее, чем
`@click.self` overlay, т.к. popover не перекрывает контент). **Без overlay** — меню легко,
не блокирует редактор.

### 2. Связка с `InputPanel.vue`
- Решение по `correct-button`: **оставить** `correct-button` как быстрый доступ к AI-коррекции
  (привычный), **и добавить** `EditorMenu` рядом. Меню — для остальных действий. (Альтернатива:
  убрать `correct-button`, перенеся «AI» в меню — но это ломает привычный UX; в этом плане
  оставляем оба, фиксируем.)
- `EditorMenu` позиционируется рядом с `correct-button` (или над ним). CSS-vars тема.
- Обработчики:
  - `@correct` → существующий `correctText()` (переиспользовать, не дублировать).
  - `@complete` → новая тонкая обёртка `completeText()`: `invoke('get_ai_completion', { context: text.value })`
    и вставка результата в `text` (аналог autocomplete, но по всему тексту / явный вызов).
  - `@toggle-history` → переключить видимость `PhraseHistoryList`. Добавить `ref` `showHistory`
    и `v-if="showHistory"` на `<PhraseHistoryList>` (по умолчанию `true` — текущее поведение).
- Minimal-mode: меню-кнопка видна (как `correct-button` сейчас скрыт через `v-if="!isMinimalMode"`
  — решить: в minimal показывать компактно или скрывать; **решение для плана:** скрывать меню
  в minimal, как correct-button, чтобы не ломать узкий layout).

### 3. AI-дописать (`completeText`) — тонкая обёртка
```ts
async function completeText() {
  if (!text.value.trim()) return
  isCorrecting.value = true // переиспользовать флаг загрузки или отдельный
  try {
    const addition = await invoke<string>('get_ai_completion', { context: text.value })
    if (addition) text.value = `${text.value} ${addition}`.trim()
  } catch (e) {
    debugError('[InputPanel] AI completion failed:', e)
    showError('Не удалось дописать текст')
  } finally {
    isCorrecting.value = false
  }
}
```
> Это **явный ручной вызов** той же команды, что autocomplete (`TtsEditor.vue:176-200`).
> Логику не дублируем — та же `get_ai_completion`.

## Риски и решения (из stage 07 + review-019)
1. **Дублирование с autocomplete (план 73)** — меню = явный вызов `correct_text`/
   `get_ai_completion`. ✅ Не дублируем логику, переиспользуем команды.
2. **Конфликт с drafts-UI (план 75)** — «История фраз» опирается на существующий
   `PhraseHistoryList`. ✅ Переиспользуем, не свой формат.
3. **Click-outside / escape** — popover закрывается (document listener + Escape). ✅
4. **Minimal-mode** — меню-кнопка скрыта в minimal (`v-if="!isMinimalMode"`), как
   `correct-button`. Зафиксировать.
5. **Онлайн-орфография** — НЕ в этот план. Будет поверх плана 78 (общий spell-linter-слой) +
   бэкенд-команда `check_spelling_online`. Пункт меню добавится там.
6. **Доступность (a11y)** — `aria-expanded`, `aria-haspopup` на триггере; focus-management
   при открытии (фокус на первый пункт). Зафиксировать как требование.

## Критерии готовности
- `EditorMenu.vue`: dropdown, click-outside + escape, тема CSS-vars, a11y (aria).
- `InputPanel.vue`: монтирование `EditorMenu`, обработчики correct/complete/toggle-history;
  `completeText()` обёртка над `get_ai_completion`; `showHistory` toggles `PhraseHistoryList`.
- Переиспользование: `correctText()` и `PhraseHistoryList` не дублируются.
- Minimal-mode: меню скрыто (как correct-button).
- `npx vue-tsc --noEmit` — 0 ошибок, 0 warnings. (Бэкенд не трогается — команды есть.)

## Объём
Малый-средний, **фронтенд-only** (бэкенд-команды `correct_text`/`get_ai_completion` уже есть).
По WORKFLOW — через DeepSeek.

## Зависимости / порядок
- **После плана 78** (если пункт орфографии добавляется) — но этап 1 (этот план) орфографию
  не включает, так что **может идти параллельно с 78**. Зависимость только от плана 75
  (PhraseHistoryList — уже реализован). ✅
- Онлайн-орфография (этап 2 stage 07) — отдельный план, после 78.

## После реализации
Code-review + арх-ревью через сабагентов. Проверить especially: click-outside корректность
(не закрывает при клике внутри), escape, утечки document-listener (onUnmounted), minimal-mode,
что не дублирует autocomplete-логику.
